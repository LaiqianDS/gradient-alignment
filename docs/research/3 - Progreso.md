## Plan original (27 abril → 22 junio 2026): retrospectiva de lo completado
### Semana 20-24 abril (retrospectiva)
- [x] Búsqueda inversa de papers: ver más recientes para mapear qué está hecho y qué no
- [x] GitHub repo
- [x] EBRON: título más amplio, resumen, palabras clave

### Semana actual (27 abril - 3 mayo). Setup y decisiones
- [x] Búsqueda inversa de papers
- [x] GitHub repo
- [x] EBRON: título, resumen, palabras clave
- [x] Decidir modelos (FC, CNN simple, ResNet) y datasets (MNIST, CIFAR-10, CIFAR-100) finales
- [x] Revisar papers e identificar familias de métricas
- [x] Montar pipeline base: carga datos, modelos, bucle entrenamiento, logging (W&B/TensorBoard), semillas fijas
- [x] Montar setup LaTex
- [x] **Entregable:** Repo entrena modelos×datasets con loss/accuracy logged

### Semanas 1-2 (4-17 mayo). Métricas de alineación
- [x] Implementar todas las métricas elegidas
- [x] Sanity checks sintéticos (gradientes paralelos → cosine ~1, random → ~0)
- [x] Granularidad: global por batch primero; por capa si hay tiempo
- [x] **Entregable:** Métricas integradas + tests sanity documentados
- [x] **Criterio éxito:** Valores coherentes ✓ (tests sintéticos; 212 verdes a 2026-08-01) y overhead <3-4x ✓. El ratio se leyó del pilot desde `metric_seconds`/`train_seconds` en `summary.json` (separados por run desde el 2026-06-10): peor caso fc × cifar100, ~3,21x el wall-clock de un run sin instrumentar, dentro de la cota (corregido el 2026-08-05: la lectura anterior citaba fc × tiny_imagenet a 2,08x, que es el run de mayor coste **absoluto** pero no el de mayor cociente). Cerrado con la decisión de coste de instrumentación del 2026-07-17 ([[2 - Decisiones]]).

## Plan por fases (actualizado 2026-06-12)

Sustituye a las semanas 3-7 del plan original, obsoletas desde que la rejilla completa fijó el horizonte de septiembre (2026-06-09; el antiguo "Plan B septiembre" pasa a ser el plan). Las fases van en orden de dependencia, no de fechas: la 1 corre en paralelo a la 0, y dentro de la 1 los cuatro frentes son independientes entre sí.


### Fase 0: confirmación del tutor (bloqueante externo)

- [x] Protocolo de evaluación: confirmado por el tutor el 2026-06-12 (particiones típicas de cada dataset, sin validación cruzada, val para convergencia, test al final) → implementado el mismo día, ver fase 2 y [[2 - Decisiones]]
- [x] Gap de generalización: confirmado por el tutor el 2026-06-14 e implementado el mismo día (tercer constructo de VD, el gap test−train; ver [[2 - Decisiones]] y VD5-VD6 en [[1 - Diseño]])

### Fase 1: trabajo no bloqueado (en paralelo a la fase 0)

1. **Pipeline de análisis** (`src/analysis/` o equivalente): implementar el plan estadístico en código antes de congelarlo: carga de `reports/`, Spearman por celda con censura por rangos, Wilcoxon cross-celda (etapa 2), familias BH + cota BY, parciales de H2, no-inferioridad de H4, binomial de H5, figuras preespecificadas.
	- **Verificación (dry-run del preregistro):** datos sintéticos con efecto plantado (ρ conocido) → el pipeline lo recupera; con ρ = 0 → falsos positivos ≈ nominal tras BH. Si algo del plan no es computable tal como está escrito, se descubre aquí, antes de congelar, no sobre los datos reales.
2. **Cola de lectura, priorizada por lo que bloquea la congelación.** Primero los 6 to-read de la tabla de signos: GSNR (Liu), Coherent Gradients + Making Coherence (m-coherence), GWA (Hölzl), GNS (McCandlish), TSE (Ru); la tabla del [[4 - Análisis]] exige revisar signos contra los papers al congelar. Después, las citas "de memoria" de los pending docs sin nota propia (Jiang 2020, Bouthillier 2021, Holmes & Friston, Lakens, Zou). Los de optimizadores (Adam, RMSProp) pueden esperar al estado del arte.
	- **Entregable:** tabla de signos verificada contra los papers + citas de los pending docs comprobadas.
3. **Redacción de lo que no depende de resultados:** estructura real en `thesis/main.tex` (hoy es la plantilla de ejemplo), introducción, estado del arte (desde las notas de `Papers/`), metodología (desde [[1 - Diseño]] y [[2 - Decisiones]]; el contenido ya está escrito, es pasarlo a memoria). Target 50-80 páginas, claridad > extensión.
4. **Logística de cluster:** entorno reproducible en nodo (uv), datasets descargados, scripts de lanzamiento (job arrays / troceado por dataset), smoke test de 1 run real en GPU.
	- **Criterio de éxito:** un run de la matriz corre en el cluster de principio a fin sin intervención manual.

### Fase 2: implementar el protocolo de evaluación (al cerrar la fase 0)

- [x] Split de 3 vías (`data.py`: 3 loaders, split estratificado con semilla fija, tamaños convencionales por dataset) + lecturas suavizadas de VD1/VD3 y test único final con F1-macro (`train.py`); implementado el 2026-06-12, ver [[2 - Decisiones]]
- [x] **Verificación del protocolo:** 166 tests verdes (split determinista/estratificado, mediana-3, umbral insensible a picos) + run corto de MNIST con las columnas nuevas en `summary.json` y lecturas verificadas a mano
- [x] Registrado en [[2 - Decisiones]] y actualizado [[1 - Diseño]] (setup de entrenamiento + VD)
- [x] Gap de generalización (pasada `evaluate()` final sobre subset fijo de train): implementado el 2026-06-14 (ver [[2 - Decisiones]]); verificado: `final_gap_loss` ≈ 0,02 a 1 época y ≈ 0,09 a 12 (positivo y monótono)

### Fase 3: pilot de calibración + congelación del plan

- [x] Lanzar los 24 runs (con el protocolo de la fase 2 ya dentro) y leer el report:
	```bash
	uv run python src/run_pilot.py            # reanudable; --dataset para trocear por nodo
	uv run python src/run_pilot.py --status
	uv run python src/run_pilot.py --report   # tabla de calibración por dataset
	```
- [x] Calibrar con los criterios preescritos (decisión 2026-06-09): presupuestos/umbrales finales escritos en los 24 YAML **y** `config.py::DATASET_BUDGET`. Registrados con su evidencia en [[2 - Decisiones]] (2026-07-17)
- [x] Chequeos adicionales que el pilot cierra: overhead <3-4x (`metric_seconds`/`train_seconds`), ninguna métrica falla sistemáticamente, redundancia GNS ≈ B·NGV, centrado de la rejilla de LR, GPU-h proyectadas para ~960 runs, suelo de ajuste del gap (distribución de `final_train_eval_acc`)
- [ ] **Congelar el plan de análisis:** valores del pilot + tabla de signos verificada → mover [[4 - Análisis]] a `docs/research/`, registrar la congelación con fecha
- [ ] **Criterio de éxito:** presupuestos/umbrales registrados con su evidencia; plan congelado y fechado. A partir de aquí no se mira ningún resultado de matriz sin plan congelado

### Fase 4: matriz completa (~960 runs)

- [ ] Lanzar por tandas (troceado por dataset/nodo), monitorizar con `--status`, relanzar pendientes (el launcher reanuda)
- [ ] QA continuo, descriptivo y sin mirar hipótesis: recuento de divergencias y censura por celda, missingness por métrica (>5% en una celda → marcarla, según el plan), runs sin `summary.json` → relanzar
- [ ] Backup incremental de `reports/` fuera del cluster
- [ ] **Criterio de éxito:** 24/24 celdas completas, dataset íntegro y copiado

### Fase 5: análisis

- [ ] Ejecutar el pipeline (validado en fase 1) sobre los datos reales: mapa descriptivo por celda + contrastes confirmatorios H1-H6 + figuras preespecificadas (val↔test, ρ vs ventana, heatmaps por familia)
- [ ] Todo lo no preespecificado se reporta etiquetado como exploratorio
- [ ] Poda de métricas redundantes con prueba (GNS≈B·NGV, clúster del Gram) para la lista *reportada*
- [ ] Si hay señal fuerte y sobra tiempo: intervención simple (LR adaptativo por alineación) vs baseline; opción de cierre: confirmar la métrica seleccionada en seeds no miradas
- [ ] Si no hay señal: resultado negativo documentado con su nota de potencia, resultado válido del diseño
- [ ] **Entregable:** tablas y figuras finales + texto de resultados con etiquetas confirmatorio/exploratorio

### Fase 6: redacción final y cierre

- [ ] Resultados + discusión (las limitaciones ya están redactadas en los docs metodológicos) + conclusiones
- [ ] Notas de honestidad de los pending docs a la memoria (Tiny-ImageNet val-como-test, F1 ≈ acc, split fijo compartido)
- [ ] Formato UPV/ETSINF, anexo ODS (1-2 páginas), Turnitin
- [ ] Borrador al tutor → incorporar feedback → entrega EBRON
- [ ] Slides defensa (10-15, ~15 min)
- [ ] **Entregable:** memoria entregada + presentación lista

## Estado actual (rev. 2026-08-01)

- **Decisiones:** Fase 0 cerrada: protocolo de evaluación confirmado e implementado el 2026-06-12 y gap de generalización confirmado e implementado el 2026-06-14 (ver [[2 - Decisiones]]). El 2026-07-17 se cerraron además la decisión de coste de instrumentación (mantener la medición completa), el registro formal de presupuestos/umbrales del pilot y la verificación de la tabla de signos contra los papers (todo en [[2 - Decisiones]]). Queda una pendiente: congelar el plan de análisis. El 2026-08-01 deja de estar en espera deliberada y pasa a ser la tarea prioritaria, porque es lo único de la lista que deja de ser creíble en cuanto existan datos en disco; sus requisitos restantes están en §Pasos inmediatos.
- **Repo:** Pipeline single-run completo con el protocolo de evaluación dentro: split train/val/test estratificado fijo, monitorización por val, test único final con F1-macro, gap de generalización sobre subset fijo de train, y lecturas suavizadas mediana-3. Métricas en `src/metrics/` con runner integrado, launchers de matriz y pilot, y timing de dos relojes. Backend de análisis en `src/analysis.py` (loaders + diagnósticos de métricas) con notebook de sanity sobre el pilot. Aviso para no confundirlo con la Fase 1.1: ese backend es **solo sanidad** (validez, identidades cruzadas, degeneración, tendencia, redundancia); el **pipeline confirmatorio no está escrito** (no hay Wilcoxon cross-celda, ni BH/BY, ni parciales de H2, ni no-inferioridad de H4, ni binomial de H5), y es el trabajo natural para los ~6 días en que la matriz corra sola.
- **Pilot:** Ejecutado y leído; la calibración se cerró el 2026-06-17. El número de épocas y los umbrales se determinaron sobre `reports_pilot/` (meseta de val-loss, cruce del umbral en val y timing), datos válidos para todos los datasets incluido Tiny pese al bug de test/gap. Presupuestos finales en épocas, escritos en `config.py::DATASET_BUDGET` y en los 24 YAML: mnist 20, cifar10 40, cifar100 40, tiny_imagenet 40. Umbrales de accuracy: mnist 0.97, cifar10 0.65, cifar100 0.35, tiny_imagenet 0.20. Criterio: cifar10 se bajó de 0.75 para recuperar a CNN, que quedaba censurada; tiny_imagenet de 0.25 para ensanchar el margen de CNN (0.007 era frágil); cifar100 y tiny_imagenet se recortaron del pico de meseta. Coste proyectado de la matriz: de ~250 a ~150 GPU-h, con tiny_imagenet en ~63% del total. Registrados con su evidencia en [[2 - Decisiones]] el 2026-07-17.
- **Bug de test de Tiny ImageNet (cerrado 2026-06-17):** El split de test usaba `ImageFolder(val/)` sobre un `val/` plano, mandando las 10.000 imágenes a una sola clase (test-acc azar ~0.5%, gap inflado ~0.43 fijo en todo el dataset). `data.py` ahora etiqueta el val oficial leyendo `val_annotations.txt` y mapeando cada wnid con el `class_to_idx` del train. Verificado entrenando 8 épocas (test 0.251 ≈ val 0.249, gap 0.079) y con una regresión hermética en `tests/test_data.py`. No hace falta re-correr el pilot de Tiny: el bug solo tocaba los campos de test/gap, y la calibración usó val y tiempos, que nunca estuvieron rotos. Separación de fuentes para Tiny: las épocas y umbrales salieron de `reports_pilot/` (lado val + timing, sanos); el **test-acc/gap corregido** (re-run post-fix a 40 épocas) vive desde el 2026-07-17 dentro de cada run del pilot (`testfix_40ep/`; la antigua `reports_validity/` quedó retirada tras la fusión; salvedad en [[Datos experimentales]] §5.1).
- **Coste de instrumentación (decisión cerrada 2026-07-17: mantener la medición completa):** El peor caso medido es fc × cifar100, con 439 s de métricas frente a 199 s de train, es decir un run instrumentado que cuesta ~3,21x lo que costaría sin instrumentar, dentro de la cota <3-4x aunque cerca de su extremo inferior; cuatro runs de FC pasan de 3x. (Corregido el 2026-08-05: la lectura anterior daba fc × tiny_imagenet a 2,08x, que es el run de mayor coste absoluto (2153 s de métricas) pero no el de mayor cociente, porque su entrenamiento también es el más caro.) la prioridad es la serie temporal completa por época. Registrada en [[2 - Decisiones]]; al congelar el plan, incorporarla a su texto.
- **Matriz (~960 runs):** Aún no lanzada; lista para arrancar en cuanto se congele el plan. Contexto de cómputo corregido (2026-07-17): hay una única GPU, no un cluster; ~147 GPU-h proyectadas ≈ ~6 días de GPU continuos, ejecutando por tandas con la reanudación del launcher.
- **Lectura:** 6/16 papers (`status: read`, según el último registro). La tabla de signos quedó verificada contra los 6 PDFs prioritarios el 2026-07-17 (subagentes; veredictos y citas en [[2 - Decisiones]]): la lectura humana de esos papers sigue pendiente y es valiosa para el estado del arte, pero ya no bloquea la congelación.
- **Memoria:** Estructura propia en marcha; redactados intro, estado del arte y fundamentos (2026-06-16) y metodología arrancada (hipótesis H1-H6 y setup). El 2026-07-17 se añadió a metodología la sección de calibración de presupuestos y umbrales (protocolo del pilot, criterios preescritos, valores finales y coste). Compila limpio (41 páginas).
- **Lista de métricas:** Cerrada con la implementación: variabilidad (normalized variance, GNS simple, GSNR) + alineación/coherencia (m-coherence, stiffness, gradient disparity, gradient confusion, GWA), más TSE como baseline obligatorio.

## Pasos inmediatos (rev. 2026-08-01)

**Lugar único de pendientes.** Desde el 2026-08-01 esta sección es el **único** sitio donde viven las tareas por hacer. `pending/Próximos pasos.md` quedó plegado aquí y eliminado: era una lista paralela en un directorio que está en `.gitignore`, así que no tenía historial ni existía para nadie que clonase el repo. La sección §Qué falta para congelar del [[4 - Análisis]] se conserva donde está, porque es la lista de puertas del propio preregistro y muere con el acto de congelar; su contenido está espejado abajo.

**Marcapasos del calendario:** La cadena que queda es congelar el plan → lanzar la matriz (~960 runs, el tramo más largo de septiembre, ahora en una única GPU) → análisis. Lo que bloquea es la congelación. Contexto de fecha: el 2026-08-01 quedan ~2 semanas de colchón sobre la fecha límite de arranque de mediados de agosto, no más.

**Ya cerrado** (hacia el arranque de la matriz; se conserva marcado, no se elimina):

- [x] **Protocolo de evaluación** confirmado e implementado (2026-06-12) y **gap de generalización** (2026-06-14); detalle en el plan por fases (Fase 0 y Fase 2) y en [[2 - Decisiones]].
- [x] **Pilot de calibración** ejecutado y leído; presupuestos/umbrales finales escritos en `config.py::DATASET_BUDGET` + los 24 YAML (2026-06-17).
- [x] **Bug de test de Tiny ImageNet** corregido y verificado (2026-06-17): test 0.251 ≈ val 0.249, gap 0.079; regresión hermética en `tests/test_data.py`.
- [x] **Referencia test/gap corregida de Tiny**: 6 celdas re-corridas con el fix (a 40 épocas), fusionadas el 2026-07-17 dentro de cada run del pilot (`reports_pilot/<run>/testfix_40ep/`; `reports_validity/` retirada); los summaries corruptos llevan la clave `_tiny_test_note`; salvedad en [[Datos experimentales]] §5.1.
- [x] **Escritura atómica de `summary.json` (2026-08-01).** `logger.save_json` usaba `path.write_text(...)`, que no es atómico: un corte durante la escritura (OOM killer, `kill -9`, caída del proceso) dejaba un JSON truncado, y como `run_matrix.Run.is_done()` solo comprueba que el fichero *exista*, ese run quedaba marcado como hecho para siempre con datos corruptos, descubribles solo semanas después al cargarlos. Arreglado escribiendo a un temporal hermano y haciendo `os.replace` (rename atómico en POSIX): el lector ve el fichero viejo completo o el nuevo completo, nunca uno a medias. `tests/test_logger.py` fija la propiedad observable simulando una muerte a mitad de escritura, y se verificó que falla contra la versión no atómica. Suite en 212 verdes. Salvedad: la garantía es de *visibilidad*, no de *durabilidad*; un corte de luz o un panic del kernel no están cubiertos (haría falta `fsync` sobre el temporal y sobre el directorio, a coste de un fsync por run).

**Para congelar la preregistración** (la decisión de instrumentación va *dentro* del preregistro, así que precede al acto formal de congelar; lanzar la matriz va al final):

- [x] **Decisión de coste de instrumentación cerrada (2026-07-17): mantener la medición completa.** Registrada en [[2 - Decisiones]]; al congelar, incorporarla al texto del preregistro.
- [ ] **Cerrar el presupuesto total de cómputo (contexto corregido 2026-07-17: GPU única, no cluster):** ~147 GPU-h ≈ ~6 días de GPU continuos (tiny_imagenet ~64%); confirmar que el calendario de septiembre lo absorbe (arrancar la matriz como muy tarde a mediados de agosto deja margen).
- [x] **Tabla de signos verificada contra los 6 PDFs (2026-07-17).** Cambios principales: m-coherence vs VD1 sigue − pero su base fuerte es Chatterjee 2020 (CGH), no Chatterjee & Zielinski; GSNR vs VD4 baja a extrapolada (su fuerte es el gap); GWA vs gap baja a direccional cualitativa (su fuerte es VD4); GNS + confirmado con condiciones. Tabla corregida en [[Datos experimentales]] §5.3 y en el plan; veredictos y citas en [[2 - Decisiones]].
- [x] **Framing de H2 alineado (2026-07-17):** [[Métricas]] (bloque TSE) y [[1 - Diseño]] (H2 y protocolo de análisis) condicionan ya por el trío TSE-EMA + `val-loss@f` + `val-acc@f`, con la parcial de Spearman como contraste primario.
- [x] **Presupuestos/umbrales del pilot registrados en [[2 - Decisiones]] con su evidencia (2026-07-17).** El registro deja explícito lo que no cierra: el suelo de ajuste del gap sigue sin valor fijado.
- [x] **Cobertura de columnas del plan verificada (2026-08-01).** Era la única carencia que habría obligado a re-correr la matriz. Comprobado contra el esquema real de un run del pilot: las seis VD están en `summary.json` (`epochs_to_threshold`, `val_loss_auc`, `best_val_loss`, `final_test_acc`, `final_gap_loss`, `final_gap_acc`), más `final_train_eval_acc`/`_loss` para el suelo y la parcial del gap y `final_val_acc` para la figura de sanidad val↔test; los ocho predictores y los tres del nivel 0 (`val_acc`, `val_loss`, `tse/ema_0_9`, `tse/ema_0_999`) están en `metrics_at_window.parquet`, con la columna `window` tomando exactamente 0,05/0,10/0,25/0,50/1,00; el indicador de validez por (run, época) de la regla de censura tiene sus dos ingredientes (`train_loss` y las columnas de coseno); el comparador power-law de VD1 lee la curva de `val_acc` por época de `trajectory.parquet`; y `seconds_to_threshold` convive con el `metric_seconds` acumulado para la corrección exploratoria del wall-clock. No falta ninguna columna. El dry-run completo del pipeline sobre datos sintéticos con efecto plantado (Fase 1.1) sigue pendiente y puede ejecutarse mientras la matriz corre.
- [x] **Regla de censura del lado predictor en runs divergentes (2026-07-25).** Verificado sobre un run `fc/mnist/sgd` a lr = 1,0 (la rejilla de SGD llega a 1,0, así que los divergentes son una fracción grande de los ~960): un run divergente **termina limpio y escribe `summary.json`**, de modo que el launcher no lo reintenta en bucle. Pero de sus 27 columnas de métrica, 23 salen NaN y cuatro (`stiffness/sign_global`, `stiffness/sign_within`, `stiffness/sign_between`, `confusion/frac_neg`) salen **0,0 finito**, un valor que parece medido. Sin regla, ese 0,0 sistemático en los LR altos (que son también los lentos y censurados) fabricaría correlación. Plegado en §Censura del plan: la validez del predictor se decide por (run, época) y no columna a columna.
- [x] **Suelo de ajuste del gap fijado (2026-08-01).** Umbral absoluto por dataset, reutilizando los umbrales de accuracy que VD1 ya usa sobre val pero aplicados sobre train: MNIST 0,97; CIFAR-10 0,65; CIFAR-100 0,35; Tiny 0,20. No se calibró sobre el pilot, y el plan explica por qué: el pilot da **un** valor por celda, al LR central, a la seed 0 y al presupuesto **doblado**, así que está sesgado al alza y no cubre el barrido de LR de 3,5 décadas, que es justo lo que puebla esa distribución en la matriz (valores observados: MNIST 0,999-1,000; CIFAR-10 0,867-1,000; CIFAR-100 0,701-0,9998; Tiny 0,350-1,000, y 0,380-1,000 en las re-corridas de `testfix_40ep/` al presupuesto real). Reutilizar un número ya congelado y ya justificado añade cero grados de libertad, y el argumento es de una frase: un run que no alcanza sobre train la accuracy que el estudio exige sobre val no aprendió. Declarado como filtro de "no aprendió" y no como calibración fina; la carga del confusor la lleva la parcial por `final_train_eval_loss`.
- [x] **Regla de matriz incompleta añadida al plan (2026-08-01).** Una celda entra en la etapa 2 solo si está completa; las incompletas se excluyen y se reportan; el análisis confirmatorio no se ejecuta con menos de **18 celdas elegibles** y H5 exige sus 12 pares. El 18 está calculado, no estimado: con 12 celdas la potencia bajo BH al SESOI del propio plan es 0,69 (ver la auditoría de potencia abajo). Se añadió además que el orden de ejecución de la matriz se fija y se registra **antes** de lanzarla, para que qué celdas acaban completas no sea una elección posterior a ver resultados.
- [x] **Política de enmiendas y salvedad de implementación añadidas al plan (2026-08-01).** Toda modificación posterior a la congelación se anota con fecha y motivo, se marca como enmienda y se declara en la memoria; una enmienda posterior a ver resultados degrada a exploratorio el contraste que toca. Y queda dicho que congelar el texto no congela el código: los empates en Spearman y el trato de los bloques de rangos empatados por censura siguen abiertos, y los cierra el dry-run con efecto plantado, que es parte del plan y no un extra.
- [x] **Auditoría de potencia del plan (2026-08-01), con las skills de estadística.** Recalculada por simulación Monte Carlo y reproducible en `src/power_analysis.py` (+ `tests/test_power_analysis.py`, 16 tests; suite en 228 verdes). Confirma el punto central del plan (24 celdas, mediana ρ = 0,30 → potencia 0,993 bajo BH) y corrige cuatro huecos. La nota calculaba a α = 0,05 **ignorando la multiplicidad** cuando el criterio decide a q, así que se añaden los α efectivos (BH 0,00625, BY 0,00230). Se documenta el **suelo discreto** del Wilcoxon (p mínimo = 2^(1−n)): con menos de 9 celdas el test no puede rechazar bajo BH tenga el efecto que tenga. **H2 gana un brazo de equivalencia TOST** con δ_H2 = 0,15 anclado en el coste de instrumentación, porque su potencia sobre parciales es mucho menor que sobre ρ crudo (0,46 si la parcial real es 0,15) y sin él el negativo que la tesis anuncia como contribución era indistinguible de la falta de potencia. Y **H4 y H5 reciben nota propia**: H5 solo puede confirmar invariancia casi perfecta (potencia 0,39 a concordancia 0,75) y H4 pasa a reportar siempre la sd(d) de la que depende. Además el tamaño de efecto de la etapa 2 pasa a ser la pseudomediana de Hodges-Lehmann con IC exacto, y el plan gana una nota de reproducibilidad.
- [x] **Revisión de la cadena objetivo → hipótesis → test (2026-08-01).** Detalle en [[2 - Decisiones]]. Lo importante: el criterio de **H3 estaba sesgado** a favor de la familia que el título apuesta (alineación tiene 5 métricas y variabilidad 3, así que el argmax cae ahí con probabilidad 5/8: 15 celdas de 24 esperadas por azar y un 0,42 de superar el umbral de 16 sin efecto alguno), y se sustituye por la diferencia pareada de medianas por familia. **H6 no tenía criterio de falsación** y gana el binomial de signos. **H5** se restringe a las métricas que pasan H1 y declara la no independencia de sus pares; se corrige además su enunciado en [[1 - Diseño]], que presentaba la invariancia como consecuencia lógica de la decisión raw-grad cuando es una afirmación empírica independiente. **H2** pasa a condicionar por `val-acc@f` sola en vez de por los tres baselines casi colineales, para no gastar potencia en la hipótesis decisiva. Y §Censura gana la vigilancia de la atenuación desigual de ρ, que ningún documento recogía. Se **descartó** añadir un leave-one-cell-out por no compensar la superficie añadida. [[1 - Diseño]] queda reconciliado con una nota fechada, no con una edición silenciosa.
- [x] **Pasada de legibilidad sobre el plan (2026-08-01).** Reescritura párrafo a párrafo sin cambios de contenido, más una **guía rápida** al principio en lenguaje llano, para que el documento sea legible por el tutor y por el tribunal sin leer las ~380 líneas.
- [x] **Acto formal de congelar (2026-08-02, commit `e433377`).** El plan se movió de `pending/` a `docs/research/` y quedó bajo control de versiones. El detalle que lo hacía urgente: el plan había sido borrado de git en el commit `563d5a5` y vivía solo en un directorio ignorado, así que hasta ese commit **no existía ningún registro verificable de que precediera a los datos**; este acto es el que lo creó. El orden queda además garantizado por construcción, porque `reports/` no está en `.gitignore` (se versiona a propósito, decisión 2026-07-25) y el commit del plan precede a cualquier commit de resultados. A partir de aquí no se mira ningún resultado de matriz fuera de lo que el plan prescribe, y toda modificación del plan pasa por su política de enmiendas.

**Antes de lanzar (en este orden):**

- [ ] **Fijar el estado del código.** Commitear lo que queda sin trackear (`notebooks/`, y decidir qué hacer con `skills-estadistica.txt`) más el arreglo atómico de `summary.json`. Revisar de paso las dos líneas que aparecieron en `.gitignore` sin autor identificado el 2026-08-01 (`.agents/`, `skills-lock.json`; contenido benigno, y verificado que `reports/` sigue **sin** estar ignorado). Etiquetar el commit que va a producir la matriz: hoy `git tag` no devuelve nada, y como `summary.json` no guarda el SHA de git, esa etiqueta es el único vínculo entre los ~960 runs y el código que los produjo. Después sincronizar con la caja Linux donde corrió el pilot (los `config.yaml` de `reports_pilot/` llevan `out_dir: /home/lai/...`, así que hay dos copias del repo que pueden divergir) y allí ejecutar `git pull`, `uv sync` y `uv run pytest`.
- [ ] **Smoke test por la celda peor, no por la primera.** `--dataset tiny_imagenet --model fc --limit 1` (monta una Gram de ~14 GB en RAM de host y tiene historia de OOM; `run_matrix` ya fuerza `PYTORCH_CUDA_ALLOC_CONF=expandable_segments`) y luego `--model resnet18 --limit 1` (la más lenta), relanzando después para comprobar que el launcher las da por hechas. `enumerate_runs` recorre los datasets en orden y deja Tiny al final, que es el ~64% del coste: un fallo descubierto ahí llega el quinto día.
- [ ] **Confirmar el calendario de cómputo:** ~147 GPU-h ≈ ~6 días continuos en la única GPU (tiny_imagenet ~64%); arrancar como muy tarde a mediados de agosto deja margen para septiembre.
- [ ] **Respetar el orden de commits.** `reports/` se sincroniza por git a propósito (decisión 2026-07-25; hace además de backup incremental de la Fase 4), así que el commit del plan congelado debe preceder al primer commit de resultados, para que el propio historial certifique que el plan precede a los datos.
- [ ] **Lanzar bajo `tmux`/`nohup` y con la salida a un log.** `execute` corre los 960 subprocesos secuencialmente dentro de un único proceso, así que una desconexión mata la tanda; y los fallos solo viven en stdout (la lista de `failures` se imprime y se descarta, sin fichero de estado ni tope de reintentos), de modo que un run que falle de forma determinista se reintentará en cada relanzamiento. La reanudación por `summary.json` cubre el relanzado, no el diagnóstico.
- [x] **Orden de ejecución registrado (2026-08-01).** El plan lo exige antes de lanzar, para que qué celdas acaben completas no sea una elección posterior a ver resultados. Fijado en [[2 - Decisiones]]: primero `mnist × cnn`, después el orden natural de `enumerate_runs` (resto de MNIST → CIFAR-10 → CIFAR-100 → Tiny).
- [ ] **Primera tanda: `--dataset mnist --model cnn`** (celda barata, 40 runs en horas), no el orden natural de `enumerate_runs`. Da una celda completa con sus 8 LR y sus 5 seeds sobre la que ejecutar los diagnósticos de adecuación del diseño el primer día, en vez de descubrir un problema de diseño el quinto con Tiny a medias. No se desperdicia nada: son 40 puntos legítimos de la rejilla.

**Durante la matriz (~6 días de GPU):**

- [ ] **Escribir el pipeline confirmatorio** (Fase 1.1: Spearman por celda con censura por rangos, Wilcoxon cross-celda, familias BH con cota BY, parciales de H2, no-inferioridad de H4 con δ = 0,1, binomial de H5 sobre los 12 pares SGD↔Adam, figuras preespecificadas) **y su dry-run con efecto plantado**. Hoy `src/analysis.py` solo cubre diagnósticos de sanidad. Se desarrolla sobre datos sintéticos, no sobre `reports/`, así que no roza la regla de no mirar resultados y convierte los ~6 días de GPU en tiempo útil.
- [ ] **Diagnósticos de adecuación del diseño** (distintos de los de sanidad actuales: estos responden "¿va a funcionar el estudio?", no "¿está bien construido el instrumento?"). Son **marginales** (distribución del predictor y de la VD por separado), nunca su asociación, así que no tocan ningún contraste confirmatorio. Cuatro: rango dinámico del predictor a lo largo de la rejilla de LR dentro de una celda, medido en la ventana `f` que usa el estudio y no sobre toda la trayectoria; degeneración de VD1 por celda según la regla del plan (>80% censurados o menos de 2 valores distintos), que importa porque la nota de potencia asume ~24 celdas elegibles; estabilidad entre seeds, es decir varianza de la métrica en `f` entre las 5 seeds al mismo LR frente a la varianza entre LR (un cociente de varianzas, el más informativo de los cuatro); y tasa real de divergencia por celda y por LR, hoy verificada solo sobre un único run (`fc/mnist/sgd` a lr = 1,0). El pilot **no puede** responderlas: tiene un run por celda, sin variación de LR ni de seed.
- [ ] **QA continuo, descriptivo y sin mirar hipótesis**, con commits incrementales de `reports/`: recuento de divergencias y censura por celda, missingness por métrica (>5% en una celda se marca, según el plan), runs sin `summary.json` a relanzar, y verificación de que los `summary.json` parsean. Atajo de diagnóstico: columna ausente en un `trajectory.parquet` significa que la métrica falló en runtime; columna presente y llena de NaN significa que el run divergió.

**No bloqueante, desde ya:**

- [ ] **Sacar a `src/` el código reutilizable de figuras y de calibración.** El notebook debe llamar, no calcular. Dos extracciones: los dos helpers de ploteo de su celda 3 (~30 líneas, incluido `trajectory_grid`) a un módulo nuevo `src/plots.py`, con funciones que reciban el DataFrame ya cargado y devuelvan Axes (`analysis.py` se queda como backend sin ploteo, así lo declara su docstring; `matplotlib` ya es dependencia); y el cálculo de la tabla de calibración de `run_pilot.py::print_report`, que hoy calcula e imprime en la misma función, de modo que la evidencia que justifica los presupuestos 20/40/40/40 y los umbrales 0,97/0,65/0,35/0,20 (`plateau@`, `thr@`, `metric%`) no es alcanzable como dato desde ningún otro sitio. Conservar la propiedad que su docstring subraya: los hechos del run se leen de disco, nunca se recomputan desde el `DATASET_BUDGET` ya editado.
- [ ] **Notebook del piloto para el tutor** (variación del actual, orientada a lector externo y no a depuración): qué mide el instrumento, evidencia de que está bien construido, evidencia de calibración, qué cuesta (peor caso ~3,21x en fc × cifar100), dirección frente a teoría en ventana temprana, redundancia, y una sección explícita de **qué no puede decir el pilot todavía**. Declarar la salvedad de Tiny (los campos test/gap de `reports_pilot/` son los corruptos pre-fix; la referencia corregida está en `testfix_40ep/`).
- [ ] **Redacción en paralelo** (no depende de resultados): completar metodología y dejar listo lo que no espera datos (implementación); intro, estado del arte y fundamentos ya redactados. Al redactar resultados, podar `sec:res-pilot` (solapa con la nueva sección de calibración de metodología).
- [ ] **Lectura humana de los 6 papers verificados con subagentes** (la tabla de signos ya no lo bloquea; siguen siendo valiosos para el estado del arte). 6/16 leídos, cola priorizada abajo.


## Cola de lectura

El `status` de cada paper vive en su frontmatter (`Papers/`); esta es su foto estática, ordenada por prioridad. 6/16 leídos.

### No leídos (10)

- **[[Gradient-Weight Alignment as a Train-Time Proxy for Generalization in Classification Tasks|Hölzl 2025]]** (high): Origen de `gwa` (coseno entre el gradiente per-sample y los pesos del clasificador final; casi gratis, last-layer; Pearson 0.99 con test accuracy). Competidor más reciente (2025), uno de los 3 papers más comparables.
- **[[Making Coherence Out of Nothing At All - Measuring the Evolution of Gradient Alignment|Chatterjee & Zielinski 2020]]** (high): Origen de `m_coherence`: formalización escalable y O(m) de la Coherent Gradients Hypothesis (coherencia per-sample normalizada, rango [0, m]; 1 = límite ortogonal). Núcleo de la familia alineación.
- **[[On the Ineffectiveness of Variance Reduced Optimization for Deep Learning|Defazio & Bottou 2019]]** (high): Muestra empíricamente que SVRG no reduce la varianza en redes modernas (la ratio de varianza SVRG/SGD supera 1). Cierra el eje varianza con SVRG y Faghri: la varianza es señal diagnóstica, no un objetivo a minimizar.
- **[[Speedy Performance Estimation for Neural Architecture Search|Ru 2021]]** (high): Origen del baseline `tse_ema` (suma exponencialmente ponderada de la train loss, coste cero). Miembro del nivel 0 junto a val-acc@f y val-loss@f. Uno de los 3 papers más comparables.
- **[[Understanding Why Neural Networks Generalize Well Through GSNR of Parameters|Liu 2020]]** (high): Origen de `gsnr` (gradient signal-to-noise ratio por parámetro; GSNR alto → menor gap de generalización), con marco teórico OSGR. Eje varianza; es el inverso conceptual de la varianza normalizada de Faghri.
- **[[An Empirical Model of Large-Batch Training|McCandlish 2018]]** (medium): Origen de `gns_simple` (gradient noise scale: ruido del gradiente relativo a su norma) que predice a priori el batch size crítico. Eje varianza; caso canónico de métrica de ruido → elección de hiperparámetro.
- **[[Coherent Gradients An Approach to Understanding Generalization in Gradient Descent-based Optimization|Chatterjee 2020]]** (medium): Coherent Gradients Hypothesis: ejemplos similares producen gradientes similares, y las direcciones que muchos ejemplos refuerzan son las que generalizan. Marco conceptual de la familia alineación (su estimador operativo es m-coherence).
- **[[Adam - A Method for Stochastic Optimization|Kingma & Ba 2015]]** (low): Optimizador del sweep (SGD vs Adam), no métrica. Normaliza el gradiente por una estimación de su segundo momento (un SNR por parámetro), intuición que motiva el eje varianza del TFG.
- **[[RMSProp - Divide the gradient by a running average of its recent magnitude|Tieleman & Hinton 2012]]** (low): Optimizador adaptativo: divide el gradiente por la raíz de un EMA de su magnitud cuadrática. Related-work, no métrica ni parte del sweep; ese divisor (SNR por parámetro) es el antecedente conceptual del eje varianza.

### Leídos (6)

- **[[A Study of Gradient Variance in Deep Learning|Faghri 2020]]** (high): Origen de `normalized_variance` (NGV: varianza del gradiente relativa a su media, inverso del SNR). Eje varianza; hallazgo contraintuitivo principal: la NGV crece durante el entrenamiento en CIFAR/ImageNet en vez de decrecer.
- **[[Disparity Between Batches as a Signal for Early Stopping|Forouzesh & Thiran 2021]]** (high): Origen de `gradient_disparity` (distancia L2 media entre gradientes de batches independientes; Pearson 0.957 entre la GD train-train y la GD train-val, lo que justifica la versión train-train como proxy). Uno de los 3 papers más comparables.
- **[[Stiffness - A New Perspective on Generalization in Neural Networks|Fort 2019]]** (high): Origen de `stiffness` (coseno/signo entre gradientes per-sample de dos ejemplos: alto si actualizar con uno mejora el otro), desagregado within/between clase. Operador base de la familia alineación; decae al empezar el overfitting.
- **[[The Impact of Neural Network Overparameterization on Gradient Confusion and Stochastic Gradient Descent|Sankararaman 2020]]** (high): Origen de `gradient_confusion` (mínimo coseno entre gradientes de pares de ejemplos; más confusión = SGD más lento). Caso worst-case de la familia alineación; la sobreparametrización por anchura la reduce.
- **[[An overview of gradient descent optimization algorithms|Ruder 2017]]** (medium): Survey didáctico de variantes de SGD (momentum, Adagrad, RMSProp, Adam). Background: respalda acotar el sweep a SGD+Adam y medir sobre el gradiente bruto. No aporta métrica.
- **[[Accelerating Stochastic Gradient Descent using Predictive Variance Reduction|Johnson & Zhang 2013]]** (low): Stochastic Variance Reduced Gradient: reduce la varianza del gradiente con un control variate para acelerar SGD. Related-work del eje varianza; su supuesto de varianza decreciente falla en deep learning real → motiva medir la varianza en vez de reducirla.

## Backlog / ideas sueltas
- Toy problem para visualización pedagógica de métricas
- Revisar qué modelos plantean los papers (probablemente conjunto fijo recurrente)
- SGD básico como baseline mínimo
- Predicción de convergencia como salida secundaria del análisis
