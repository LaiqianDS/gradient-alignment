## Estado actual (rev. 2026-09-03)

Aquí viven el estado y el plan. El *qué se decidió y por qué* está en [[2 - Decisiones]] y el *estado resultante del diseño* en [[1 - Diseño]]; este documento no los repite, los referencia. Tampoco guarda el camino recorrido, que ya está en el historial de git, solo dónde está el trabajo hoy. **La fase 0 quedó cerrada el 2026-08-28** y lo siguiente es la fase A.

- **Matriz: TERMINADA.** 960 de 960 runs, las 24 celdas con sus 40 cada una, versionados en git desde el commit `f6df900` (2026-08-22), así que existen fuera de esta máquina. Coste real 121,7 h de reloj, 97,6 h de entrenamiento y 24,1 h de instrumentación.
- **Siguiente paso, acordado con Lai el 2026-09-03: dos flujos en paralelo y tres tandas** ([[2 - Decisiones]] 2026-09-03 y §Plan por objetivos). La fase B quedó cerrada el 2026-09-03 con sus cuatro pasos, y el flujo de análisis abre la tanda 2 con las fases C y E; la otra sesión toma D, F y G cuando termine lo suyo, todas sobre `results/`. El flujo de memoria, la otra sesión, sigue la revisión párrafo por párrafo en el orden fijado el 2026-09-02, Metodología, Fundamentos, Estado del arte, Introducción, Desarrollo y Trabajos futuros, con las cuatro comprobaciones por párrafo (ninguna promesa colgando, todo término explicado antes de usarse, cada número recalculado sobre `reports/`, y los tics de [[Estilo de redacción - notas del TFM HOFT]]), y escribe §Calibración del *pilot*, el diagrama del *pipeline* y el anexo de configuración; el anexo ODS quedó escrito el 2026-09-03. **Resultados quedó revisado el 2026-09-03** (siete cifras corregidas sobre `reports/`, dos incoherencias de §Rango dinámico, seis términos explicados); de ese capítulo quedan §Calibración del *pilot*, siete títulos de las fases B a G sin prosa, que se rellenan por fases, y la hoja de ruta y las conclusiones del capítulo, que esperan al cuerpo.
- **El método de análisis se fijó el 2026-09-03 por la mañana, se ejecutó por la tarde y se revisó esa misma tarde en diez puntos** tras la revisión externa de la memoria, después de la primera lectura de H1 y por motivos de estructura ([[2 - Decisiones]], entrada «Diez desviaciones»; resumen en [[1 - Diseño]] §Método de análisis). Como el plan preregistrado se retiró, el análisis es **posterior a los datos**, el método de la mañana es **anterior a cualquier coeficiente**, y las desviaciones se declaran con fecha y motivo; así hay que presentarlo en la memoria, con las tres fechas.
- **La tabla larga existe desde el 2026-09-03 y se regeneró esa tarde con el método revisado**: `results/tabla_larga.parquet` (los 806 que aprendieron) y `results/tabla_larga_960.parquet` (sensibilidad, solo en `results/`), una fila por celda, ventana, predictor y variable, con la D, sus pares y su error típico por jackknife, `D_ref` contra la referencia nombrada, la granulada estratificada con sus pares, la D por hitos con el número en riesgo para la velocidad y la fracción por delante; doce predictores, los once titulares más `log_lr`. Se regenera con `uv run python src/contrast.py`, que imprime además la tabla de selección (`selection_regret`). La lectura de H1 escrita por la tarde con el recuento de signos y Benjamini-Hochberg quedó obsoleta y se reescribe con los intervalos.
- **Código.** `src/analysis.py` es el backend de sanidad de las columnas y tiene el rango dinámico del punto 3; `src/efficiency.py` tiene por unidad el run y la celda: censo de salud, mapa de lo computable, solape del punto 4 (regla en `AHEAD_FLOOR`) y concordancia del punto 5 (banda en `NOISE_SIGMAS`). Los tamaños de las particiones viven en `config.py::VAL_SIZE` y `TEST_SIZE`. Las figuras viven en `src/figures.py`, una función por figura, ocho funciones de las que la memoria usa **cinco** (`ventana-lr`, `rango-celda`, `rango-columnas`, `solape-celda`, `signos-h1`). `solape-mapa` se eliminó del todo la noche del 2026-09-03, función, prueba y PDF, porque Lai no entendió ni el mapa de calor ni la versión de un punto por celda que lo sustituyó; los recuentos por celda y ventana viven en la prosa de §Solape. `signos-h1` es una sola fila de D por celda leyendo `results/tabla_larga.parquet`, con el punto relleno cuando el intervalo por *jackknife* excluye el cero y sin la fila granulada ni las rayas de intervalo; `solape-cruces`, `solape-bandas` y `val-test` quedaron retiradas de la memoria el 2026-09-03 y sus funciones siguen en el fichero, con estilo de artículo desde el 2026-09-02 (Okabe-Ito, TeX Gyre Heros) y rótulos en castellano desde el 2026-09-03. Suite en **302** pruebas verdes. `train.median3` deja el valor crudo en los bordes desde la tarde del 2026-09-03, y `efficiency.py` recalcula desde `trajectory.parquet` los tres campos suavizados (`smoothed_fields`), calcula la D con su jackknife y sus estratos (`d_stats`) y conserva `shape_census`, `declared_cells`, `somers_d` y `pair_agreement`; `src/contrast.py` es la máquina de la fase B revisada: `outcomes`, `long_table` (D, `se`, `D_ref`, granulada estratificada, hitos, `log_lr`), `primary_family` (hitos para la velocidad), `sign_counts` (recuento por signo y por intervalo, con desglose opcional) y `selection_regret`; `bh` y la prueba binomial se retiraron; probada sobre directorios sintéticos de efecto conocido (`tests/test_contrast.py`). `analysis.SPECS` clasifica `gd/scalar` como variabilidad.
- **Runs con NaN.** 154 nunca aprendieron (115 por colapso, 39 por divergencia) y 11 se recuperaron; los recuentos completos están en [[2 - Decisiones]] (2026-08-31). El cero falso de `stiffness/sign_*` y `confusion/frac_neg` está parcheado en `reports/` desde el 2026-08-29, **así que se comporta como cualquier valor ausente y ninguna fase posterior necesita una excepción**.
- **Qué hay realmente registrado por época.** `trajectory.parquet` trae **23 columnas de métrica de gradiente** (no 8: cada métrica emite varias claves) más **cuatro columnas de TSE**, más la curva cruda `train_loss`/`val_loss`/`val_acc` y los dos relojes. `metrics_at_window.parquet` son esas mismas columnas en cinco filas por run, una por ventana.
- **Memoria.** Los seis capítulos de cuerpo se corrigieron la tarde del 2026-09-03 con los hallazgos de la revisión externa (`docs/research/writing/Revisión externa de la memoria.md`, gitignorada): Introducción y portada (pregunta acotada a la celda, objetivos reescritos, alcance con lo congelado y las tres fechas, palabras clave sin «parada temprana»), Estado del arte (parada multi-fidelidad, proxies de coste cero, Jiang y Dziugaite, concordancia con censura e hitos, familias excluidas, tabla con el objeto medido y los techos, veinte referencias nuevas), Fundamentos (momentum y Adam, modo de evaluación, batch sin estratificar, identidad de la disparidad, GWA como margen, tres estimadores de α y sus techos, las cinco señales), Metodología (las seis hipótesis reescritas, suavizado con bordes crudos, referencia nombrada y `log_lr`, tabla de ventanas en epochs y pasos, régimen crudo, la calibración contada con la regla y sus 40/40/20/20, §Protocolo de análisis reescrito con hitos, selección, jackknife y sin binomial, trece riesgos) y Desarrollo (cuatro frases falsas corregidas, fila del barrido compartido, sobrecoste por celda, determinismo acotado, pilot con sus dos diferencias de pipeline). De Resultados están escritas las cuatro secciones de la fase A, con los números del suavizado corregido y sin las figuras `solape-cruces`, `solape-bandas` ni `val-test` (§Concordancia quedó en tres párrafos y §Rango dinámico sin la digresión de Kruskal-Wallis la noche del 2026-09-03), y §Correlaciones por condición reescrita con los intervalos (H1 se sostiene como puerta por GWA en velocidad y accuracy de test y por la disparidad en accuracy de test; la stiffness iguala a la posición en la rejilla en el gap; la granulada no pasa de tres celdas); el título vacío §Calibración del pilot se retiró y el sobrecoste va en §Qué variables. Las otras siete secciones de Resultados y Conclusiones siguen siendo títulos sin prosa. **Cinco figuras** en el cuerpo; sigue faltando el diagrama del *pipeline*. El anexo de configuración está escrito salvo la línea de la GPU, marcada en el texto como pendiente de `nvidia-smi`; el anexo ODS está escrito desde el 2026-09-03. Faltan los tres resúmenes de `main.tex` y los agradecimientos. Compila en 96 páginas (101 antes del recorte de la noche del 2026-09-03: §Riesgos en una página larga, §Protocolo y §Calibración aligerados, anexo de configuración sin lo que Desarrollo ya cuenta, §Cálculo de las métricas y §Rango dinámico recortados, una figura más pequeña y dos retiradas), sin referencias ni citas sin resolver ni cajas desbordadas. La nota del seminario recomienda entre 50 y 100 páginas, y faltan por escribir unas 10 (fases C a G, Conclusiones y resúmenes); si hay que recortar más, los candidatos son §Configuración del entrenamiento (las referencias de accuracy y la cota de GenURL), las lecturas H3 a H6 de §Protocolo y Fundamentos.
- **Qué hay escrito del censurado, con precisión.** `metodologia.tex` §Variables declara la **convención de registro**, que un valor censurado se anota como ausente y nunca como el presupuesto, y remite su tratamiento en el contraste a §Protocolo de análisis. La **población de análisis**, es decir qué entrenamientos entran, no está escrita en ningún sitio. Las dos están decididas desde el 2026-09-03 y las escribe la fase B en §Protocolo de análisis.
- **Promesas colgantes en la memoria: seis (recuento del 2026-09-03, tras escribir §Protocolo de análisis).** Las seis que apuntaban a §Protocolo quedaron cumplidas; siguen una a `ch:resultados`, dos a `ch:conclusiones` y una a `sec:determinismo` del anexo de configuración, y las dos que §Protocolo abrió hacia §Correlaciones por condición, las celdas declaradas por métrica y el suelo del *gap* por celda, las cerró el paso 4 el mismo día. Quedan cuatro. Ninguna es ya trabajo de fase 0. Las de falsación (`introduccion.tex:54` y `:68`, `estado-del-arte.tex:111`, `fundamentos.tex:170` y `:259`) las cumple la fase B y no son errores, son anuncios. **LaTeX no detecta ninguna**, porque un `\ref` resuelve igual aunque la sección esté vacía, así que «cero referencias indefinidas» no vale como comprobación y hay que contarlas aparte, comparando cada `\label` con la prosa que le sigue. Lección del recuento: escribir una sección sube tantas como baja, porque cada remisión honesta abre una colgante nueva.
- **Pilot:** ejecutado y leído; los presupuestos que salieron de él están en `config.py::DATASET_BUDGET` y en los 24 YAML. Los umbrales ya no, desde el 2026-09-01, y viven en `config.py::THRESHOLD_ACC` calculados sobre la matriz. Desde el 2026-08-29 `reports_pilot/` está versionado, con los seis `testfix_40ep/` de Tiny dentro, así que la evidencia que justifica esos números ya existe fuera de esta máquina.

## Vigentes: a tener en cuenta en todo lo que queda de análisis (rev. 2026-09-03)

Lista de comprobación para abrir cualquier sesión de análisis. Entra aquí lo que **condiciona un cálculo futuro** y se olvidaría con facilidad: trampas medidas, equivalencias entre columnas, y decisiones abiertas que cambian un número. No entra el estado del trabajo, que está arriba, ni el porqué de cada cosa, que está en [[2 - Decisiones]]. Una línea por asunto y un puntero. Cuando algo deja de condicionar, se borra de aquí.

- **El método es posterior a los datos.** La matriz acabó el 2026-08-22 y el plan se retiró el 2026-08-25. Ninguna frase, en el vault o en la memoria, puede sugerir preinscripción.
- **El umbral de VD1 es por dataset y arquitectura desde el 2026-09-01** ([[2 - Decisiones]]), y sale de una norma reproducible: el valor redondo más alto que alcanza al menos el 60 % de los entrenamientos que aprendieron. Si se recalcula, se recalcula con la norma y no a mano. Y **VD1 se recalcula siempre en la capa de análisis** desde `trajectory.parquet`: el campo `epochs_to_threshold` de los 960 `summary.json` está calculado con los umbrales viejos y **no se puede leer**. Desde la tarde del 2026-09-03 lo mismo vale para `best_val_acc` y `best_val_loss`: el suavizado corregido (valor crudo en la primera y la última *epoch*) se aplica en `efficiency.py` y los tres campos suavizados de `summary.json` son obsoletos.
- **El learning rate mueve el predictor y también la variable a predecir**, medido el 2026-09-02. Un reparto de 0,90 dice que nueve décimas de esa columna son el learning rate leído de otra manera, así que la correlación de la fase B será en buena parte una correlación entre dos efectos de una causa común. Declarado en `resultados.tex` §Rango dinámico del predictor, y hay que volver a decirlo al interpretar cada contraste.
- **El predictor de referencia gana en rango dinámico a todas las métricas caras**, TSE 0,97 y la curva de validación 0,95 frente a 0,92 la mejor instrumentada. Es un mal presagio para H2 y hay que tenerlo delante al escribir la fase C.
- **VD1 y el predictor de referencia leen la misma curva.** Descontar la validación temprana para predecir VD1 es descontar un prefijo del propio resultado. OE2 será informativo sobre todo en VD4, VD5 y VD6.
- **El solape está medido por celda desde el 2026-09-02, en pares, y acota OE4 sobre la velocidad.** VD1 sirve en la ventana del 5 % en 22 celdas y en la del 10 % en 12, y en ninguna al 25 % ni al 50 %. VD3 da 23, 17, 9 y 2 celdas, y donde falla es por sobreajuste, no por velocidad. VD2 sirve hasta el 25 % en las 24 y al 50 % cae sobre la línea por construcción, «descrita a medias». La regla es la mitad por delante (`efficiency.py::AHEAD_FLOOR`) y **la cuenta es un suelo**, porque no haber cruzado no es estar por decidir. Desde la tarde del 2026-09-03 la lectura primaria de la velocidad es **por hitos**: la D solo entre los runs que aún no habían cruzado al cerrar la ventana, censurados incluidos, con el número en riesgo al lado; la D sobre todos los runs se publica como secundaria con la fracción consumida, y la regla de la mitad ya no decide qué celdas se leen. Detalle en [[2 - Decisiones]].
- **GWA va al revés que su artículo.** Aquí se mide sobre $\nabla\ell$ bruto, así que la predicción heredada es que una GWA **baja** acompañe a mejor generalización. Conversión en `fundamentos.tex:168`, con el mismo aviso en el enunciado de H6 de [[1 - Diseño]] y en la tabla de signos de [[Datos experimentales]] §5.3, que es el material contra el que se contrasta H6.
- **En los 41 runs divergidos, `final_test_acc` y `final_gap_acc` traen número y no son mediciones**, porque `argmax` sobre logits NaN devuelve siempre el índice 0. Son las dos únicas columnas donde "no falta ningún valor" engaña. Los 41 son todos de SGD: Adam no divergió nunca.
- **`noise_scale/simple` y `mcoh/global` son la misma cantidad** (GNS = M/α − 1 exacto, Spearman intra-run −1,000), y GSNR es el recíproco por parámetro de NGV. Desde el 2026-09-03 la m-coherencia sale del contraste y la escala de ruido la representa; la predicción de signo de su artículo (H6) se lee sobre la escala con el signo cambiado. NGV y GSNR no son redundantes entre sí, |D| mediana 0,59 al 5 %, pero **NGV sale también desde la tarde del 2026-09-03**, porque es la escala de ruido estimada con diez submuestras de 25 (Spearman 0,94 dentro de celda con la escala de ruido) y la regla de la mañana nunca la comparó con ella. La *gradient disparity* pasa a variabilidad, porque es √(2·tr Σ/51). Quedan seis métricas de gradiente, tres por familia.
- **En velocidad, la familia primaria lee las 24 celdas por hitos** (`contrast.primary_family` toma `D_land` para VD1), con el número en riesgo declarado; `AHEAD_FLOOR` ya no filtra celdas (2026-09-03, tarde). Donde quedan pocos en riesgo, el intervalo lo dice.
- **`results/` guarda tablas derivadas y versionadas**, nunca datos crudos, y cada fichero se regenera con una orden desde `reports/`; los números de la memoria que salgan de ahí se citan con la fecha del fichero.
- **Cada métrica entra por su columna titular**, fijada el 2026-09-03 en la tabla de `metodologia.tex` §Variables y en `analysis.py::headline_columns()`. Las demás columnas de las 27 no se contrastan, o solo como exploratorio.
- **El estadístico de todos los contrastes es la D de Somers sobre pares comparables** (2026-09-03): valores distintos de la VD y, en velocidad, al menos un cruce; un cruce en la última *epoch* contra un censurado es comparable. Cada D lleva su error típico por jackknife (quitar un run) y un intervalo normal al 95 %; una celda muestra asociación si el intervalo excluye el cero. Agregación: cuántas celdas la muestran y con qué signo, con desglose por conjunto; el recuento de signos es descripción y la referencia binomial (18 de 24, 10 de 12) solo una vara; sin Benjamini-Hochberg (2026-09-03, tarde).
- **La posición en la rejilla, `log_lr`, es un predictor de coste cero en todas las tablas** (2026-09-03, tarde): el techo de cualquier lectura monótona del *learning rate*; una métrica que no lo supera es el *learning rate* leído de otra forma.
- **H2 se compara con una referencia nombrada de antemano**, el *accuracy* de validación para VD4 y el *loss* de validación para el *gap*, y `D_ref` mide dentro de la celda si la métrica es la referencia con otro nombre; la lectura de selección (`contrast.selection_regret`) da el *accuracy* de test que cuesta elegir el *learning rate* con la métrica al 5 % frente a elegirlo con la validación al 5 % (2026-09-03, tarde).
- **Ninguna desviación posterior a la tabla larga se justifica por un coeficiente.** La cronología (método por la mañana, tabla y primera lectura de H1 por la tarde, revisión externa sin correlaciones, desviaciones después) está en [[2 - Decisiones]] y se declara en §Cuándo se fijó.
- **Las variables dependientes y los tres predictores gratuitos hacen valle o pico a lo largo del *learning rate*, y las métricas de gradiente son irregulares** (2026-09-03, tolerancia del 5 % del recorrido). Las celdas donde una métrica monótona no puede dar relación monótona, de 1 a 7 por métrica, se declaran antes de calcular.
- **La *seed* no mueve las métricas de alineación** (0,03 a 0,08 frente a 0,13 de referencia al 5 %) **y mueve algo las de variabilidad** (0,17 y 0,12). La cuenta granulada solo tiene con qué trabajar en variabilidad; predicción registrada antes de calcularla. Desde la tarde del 2026-09-03 es estratificada (los pares dentro de *learning rate* sumados) y lleva su número de pares.
- **Empates en la velocidad:** mediana del 13 % de los pares entre cruces por celda, máximo 44 %; los pares empatados no son comparables.
- **TSE es una suma acumulada**, no el *loss* de la *epoch*: a ventana fija ordena como la media del *loss* de las primeras *epochs*.
- **154 runs nunca aprendieron y los 154 tienen causa conocida**, 115 de colapso y 39 de divergencia. Se identifican por `learned` de `efficiency.py::run_health`: mejor val-accuracy suavizada por debajo de **1,25 veces el azar** (`CHANCE_MARGIN`); 1,2 y 1,25 seleccionan los mismos 154. **Distinguir dos usos, que no son el mismo:** se excluyen del denominador que **calibra** el umbral de VD1 (norma del 2026-09-01), porque no cruzarían ninguno; desde el 2026-09-03 la población primaria de todo **contraste** son los 806 que aprendieron y los 960 son la sensibilidad declarada.
- **La censura de VD1 se cuenta en pares, no en runs.** Un run censurado sigue siendo comparable con cualquiera que cruzó. 15 cruces de 40 conservan el 61,5 % de los pares, no el 37,5 %. Con n = 40 en las 24 celdas, esa fracción es una función estrictamente creciente del número de cruces, así que da la escala honesta y **no una evidencia aparte**, porque los dos números dicen lo mismo.
- **Los 24 YAML de `experiments/` llevan el umbral viejo por conjunto de datos.** `run_matrix.py --init` no pisa ficheros existentes, así que siguen ahí desde que se corrió la matriz. No afecta a ningún cálculo, porque el análisis lee `config.py::THRESHOLD_ACC` y nunca los YAML, pero engaña a quien los abra.
- **El suelo de ajuste del gap está fijado desde el 2026-09-03:** entran en VD5 y VD6 los runs cuya `final_train_eval_acc` alcanza el τ de su celda. Es un filtro de análisis, no obliga a re-correr nada, y falta medir cuántos runs deja por celda.

## Problemas conocidos de los objetivos (rev. 2026-08-26)

Ninguno obliga a cambiar los seis objetivos de `introduccion.tex`. Se resuelven declarando alcance, y hay que resolverlos **antes** de escribir el objetivo al que afectan.

- **OE2 y la velocidad.** El baseline lee la misma curva que define la variable. VD1 es "en qué época la accuracy de validación cruza τ", y la segunda mitad del predictor de referencia es "la accuracy de validación en la ventana f". Son la misma curva leída en dos puntos, así que descontar el baseline para predecir VD1 es descontar un prefijo del propio resultado. Con el solape medido el 2026-09-02 pasa lo mismo, porque si en una ventana ya ha cruzado buena parte de los runs, esa ventana no anticipa la velocidad, la describe, y en un run ya cruzado la validación leída en la ventana vale ya el umbral. Está declarado en `resultados.tex` §Solape, y hay que repetirlo al redactar OE2 y OE4.
- **OE3 y el solape entre familias.** Comparar familia contra familia sin podar antes cuenta el mismo número una vez en cada bando, porque la escala de ruido y la m-coherencia son la misma cantidad reparametrizada y GSNR es el recíproco por parámetro de NGV. **La poda con prueba deja de ser mejora de presentación y pasa a ser requisito previo de OE3.** Hecha el 2026-09-03 y completada esa tarde: variabilidad = escala de ruido, GSNR y *gradient disparity*; alineación = *stiffness*, *gradient confusion* y GWA. NGV y la m-coherencia fuera.

## Plan por objetivos (rev. 2026-09-03; el objetivo de la primera semana de septiembre de 2026 queda superado y no hay fecha nueva: Lai fijó el 2026-09-03 que se termina lo antes posible)

**Metodología de trabajo, fijada el 2026-08-27: una cosa cada vez, y en tres carriles a la vez.** Son dos mitades que se sostienen la una a la otra, y gobiernan todo lo que queda del proyecto.

*Una cosa cada vez.* En cada momento hay un solo tema abierto, y no se abre otro hasta cerrarlo. Lo que aparezca por el camino se anota donde corresponda y se sigue con lo que había. Dispersarse deja tres cosas al setenta por ciento, y tres cosas al setenta por ciento valen cero.

*Tres carriles a la vez.* Ese tema único avanza en los tres sitios en la misma tanda: el **código** de `src/` y `tests/`, la **documentación** del vault, y la **memoria** en `thesis/`. No se adelanta el código para escribir el texto más tarde, porque el texto que se deja para más tarde no se escribe. Si un paso no toca alguno de los tres carriles, se dice por qué en vez de dejarlo en silencio.

**La regla de fase, y no se negocia: una fase, un objetivo, un entregable completo.** Una fase se cierra cuando su código está escrito y probado, sus números calculados sobre `reports/`, su figura hecha, y su texto redactado en el `.tex` correspondiente. Nada pasa a la fase siguiente con algo a medias, porque lo que queda a medias es lo que no se hace.

**Segunda regla, del 2026-08-26:** toda cuenta que se ejecute tiene que poder responder de qué objetivo es y qué se haría distinto si saliera al revés. Si no hay respuesta, no se ejecuta. Y el contraste no puede ser solo visual: cada objetivo lleva su prueba de hipótesis, que hay que reconstruir porque la anterior se retiró.

**Regla de figuras, del 2026-08-26:** una figura, una afirmación, con la evidencia numérica visible. Si no se puede resumir en una frase que empiece por "esta figura demuestra que", está mostrando datos y no un resultado, y su sitio es un anexo o el repositorio.

**Regla de capítulo, del 2026-08-27:** la plantilla de [[Estilo de redacción - notas del TFM HOFT]] rige en los capítulos de cuerpo, del 2 al 6, y no en la Introducción, las Conclusiones ni Trabajos futuros, que cierran solos. La hoja de ruta y las conclusiones de un capítulo se escriben junto a su cuerpo y **nunca antes**, para no dejar un título sin prosa. Solo falta la de Resultados, y la instrucción está como comentario en ese fichero.

**Regla de registro, del 2026-08-29:** el vault se poda mientras se trabaja, no al final. Una entrada de [[2 - Decisiones]] guarda cuatro cosas y ninguna más: qué se decidió, por qué en la forma más corta que aún lo defienda ante un tribunal, la evidencia medida con su número, y las trampas que le costarían tiempo a quien lo toque después. Se va todo lo demás: el camino hasta la decisión, las alternativas que ya no informan nada, el detalle de la verificación, y lo que el código o la memoria ya cuentan mejor. **Lo que no se poda nunca son las decisiones y sus fechas**, porque son la evidencia del encuadre que la fase H tiene que declarar, que el análisis es posterior a los datos y anterior a los resultados.

**Esto es estructura, no compromiso.** Lo único fijo son las reglas de arriba. Todo lo que sigue es un esqueleto para no perderse, y **cada fase puede cambiar**: su método, su contenido, sus entregables, su orden, y una fase puede partirse en dos o desaparecer. Cada fase se abre decidiendo qué se hace en ella y registrando esa decisión en [[2 - Decisiones]] antes de programar nada. Si al abrir una fase resulta que las siguientes ya no tienen sentido tal como están escritas aquí, se reescriben aquí y se sigue: este documento va detrás del trabajo, no delante.

### Fase 0: poner el LaTeX al día

**Cerrada el 2026-08-28.** Puso la memoria al día con todo lo que ya se sabía, sin depender de ningún resultado: plantilla de capítulo, estilo de figuras, terminología en inglés, los capítulos de Metodología, Desarrollo y Trabajos futuros, y la revisión de Lai sobre el texto. Cada decisión está en [[2 - Decisiones]], y lo que sigue vivo de esta fase está arriba, en §Estado actual y en las reglas.

### Fase A: datos y validez

No ataca ningún objetivo. Establece que los datos sirven y qué se puede calcular con ellos. **Regla de ejecución: un lado cada vez**, predictores y variables dependientes por separado.

Plan de ejecución, decidido el 2026-08-31: `src/analysis.py` para las columnas y `src/efficiency.py` para runs y celdas; orden 1 y 2, luego 3 (lado predictor) y 4 y 5 (lado variable dependiente), porque el 3 y el 4 descartan dos de las tres causas de un coeficiente nulo antes de mirar ninguna correlación. El método de cada punto se decidió al llegar a él y está en [[2 - Decisiones]] con su fecha; la evidencia completa y sus lecturas están en `resultados.tex`.

- [x] **Puntos 1 y 2 (del 2026-08-29 al 2026-09-01):** columnas válidas sobre los 960 y un cero falso parcheado en `reports/`; censo de runs y mapa de lo computable; umbral por conjunto de datos y arquitectura, y figura `ventana-lr`. Escrito en `resultados.tex` §Qué variables quedan medidas.
- [x] **Lección del carril de LaTeX (2026-09-01, repetida el 2026-09-03):** los números del vault se vuelven a medir sobre `reports/` antes de entrar en la memoria, no se copian. Dos salieron mal el día 1 y uno más el día 3.
- [x] **Punto 3, rango dinámico (2026-09-02):** la causa dos de un coeficiente nulo queda descartada en casi toda la rejilla. §Rango dinámico del predictor, figuras `rango-celda` y `rango-columnas`.
- [x] **Punto 4, solape (2026-09-02):** acotada, no descartada; para la velocidad solo las ventanas del 5 y el 10 % anticipan algo. §Solape entre la ventana y el desenlace, figuras `solape-celda`, `solape-cruces`, `solape-bandas` y `solape-mapa` (de las cuatro solo queda `solape-celda` en la memoria desde el 2026-09-03).
- [x] **Punto 5, concordancia validación-test (2026-09-02):** las variables leídas sobre validación no son un artefacto de la partición. §Concordancia entre validación y test, figura `val-test` (retirada de la memoria el 2026-09-03 por la noche; la sección quedó en tres párrafos con sus números).
- [x] **Decisión de las nubes de puntos, cerrada el 2026-09-03 sin mirar ninguna.** La forma se lee lado a lado a lo largo del *learning rate*, la cuarta vía, y las celdas donde tau no puede ver nada se declaran por métrica antes de calcular. Números y trampas en [[2 - Decisiones]].
- [ ] **Criterio de cierre:** veredicto de validez escrito, mapa tabulado, y las decisiones que se deriven reflejadas en el `.tex`. Los cinco puntos y la decisión de las nubes están cerrados; la fase se cierra del todo cuando el recuento de formas entre en `efficiency.py` con prueba y su párrafo en §Protocolo, que es el primer paso de código de la fase B.

**Las fases B a G son una máquina y seis consultas (visto el 2026-08-31, máquina fijada el 2026-09-03).** No son seis trabajos. La fase B construye el contraste entero y lo estrena con OE1; las otras cinco añaden una idea cada una encima de la misma maquinaria, y OE6 no calcula nada nuevo, solo lee signos. Las ocho elecciones de esa máquina están decididas y con base en [[2 - Decisiones]] (2026-09-03) y resumidas en [[1 - Diseño]] §Método de análisis. La poda de métricas redundantes va antes de la fase B, porque decide qué columnas entran como predictor en *todos* los objetivos.

**Tandas y flujos (2026-09-03).** Tanda 1, ya: el flujo de análisis abre la fase B y el de memoria hace lo que no lee la tabla larga (§Calibración del *pilot*, la revisión de los seis capítulos, el diagrama del *pipeline*, el anexo de configuración). Tanda 2, cuando exista la tabla larga: C y E en una sesión, D, F y G en la otra. Tanda 3, en serie: H y el resto de la I. §Protocolo de análisis lo escribe la fase B para los seis objetivos; C a G solo escriben su sección de Resultados. Reparto de ficheros en [[2 - Decisiones]].

Entrada común a todas: los predictores de `metrics_at_window.parquet` (5 filas por run, una por ventana), las seis VD de `summary.json`, y el mapa de la fase A para la n de cada casilla.

### Fase B: OE1, existencia

- [x] **Paso 1, código, hecho el 2026-09-03:** el recuento de formas de la cuarta vía está en `efficiency.py` con prueba, y la poda ejecutada: `mcoh/global` sale por identidad y la representa `noise_scale/simple`; NGV contra GSNR da |D| mediana 0,59 al 5 %, bajo el 0,8, así que entran las dos y la variabilidad queda en tres cantidades. Resultado y trampas en [[2 - Decisiones]].
- [x] **Paso 2, código, hecho el 2026-09-03:** `src/contrast.py` con pruebas sobre datos sintéticos de efecto conocido antes de tocar `reports/`, porque un contraste no validado contra un efecto que se sabe que existe no distingue «no hay señal» de «tengo un bug». La tabla larga está en `results/` (ver §Estado actual): D, pares, granulada y fracción por delante por celda, ventana, predictor y VD, para las dos poblaciones. Convenciones que fija el código: la población de sensibilidad excluye los valores que no son medición (accuracy de los divergidos); el suelo del *gap* entra por `final_train_eval_acc` ≥ τ de la celda; una D se calcula igual en todas las variables y la censura solo cambia qué pares son comparables.
- [x] **Paso 3, texto, hecho el 2026-09-03:** §Protocolo de análisis entero en `metodologia.tex`, seis subsecciones (cuándo se fijó, población, coeficiente de una celda, cuenta granulada, agregación, lectura por objetivo) con la ecuación de la D, para los seis objetivos, y cierra las tres deudas y las siete remisiones abiertas; siete entradas nuevas en `references.bib` (Somers, Harrell 1982 y 1996, Newson 2002, Jiang 2020, Benjamini-Hochberg, Nosek 2018). Deja dos promesas a §Correlaciones por condición: la lista de celdas declaradas por métrica y el recuento del suelo del *gap* por celda (693 de 806 en total).
- [ ] **Trampa:** un coeficiente nulo tiene tres causas que desde fuera no se distinguen, que la métrica no prediga, que no varíe dentro de la celda más allá del ruido de semilla, o que el resultado ya hubiera ocurrido al medir. Las descartan los puntos 3 y 4 de la fase A, y por eso van antes.
- [x] **Paso 4, texto, hecho el 2026-09-03:** §Correlaciones por condición de Resultados, con H1 respondida como puerta y no como hallazgo, la Tabla de recuentos de signos y la Figura `signos-h1`. Resultado: H1 se sostiene por GWA (20 de 22 en velocidad, 4/20 en accuracy de test) y stiffness (4/20 en gap de loss) tras Benjamini-Hochberg; las otras cinco métricas son una moneda y cambian de signo con el conjunto de datos; la cuenta granulada no baja de q = 0,159 en ninguna de las 30, así que la asociación es el learning rate; los 960 no cambian ningún veredicto. Hallazgo colateral: con el gap de loss los predictores gratuitos se parten por arquitectura (ResNet-18 al revés que FC y CNN, 7 de 8 frente a 0 de 16). **Tres deudas que Resultados le deja por escrito a §Protocolo de análisis (contadas el 2026-09-03):** qué hace el estadístico con una relación en U, que un coeficiente de rangos leería como ausencia de señal (§Rango dinámico); qué hace el contraste con los entrenamientos que ya cruzaron dentro de una ventana que sirve, porque quitarlos recorta el extremo rápido y dejarlos mezcla detectar con predecir (§Solape); y cómo se interpreta el confusor del learning rate, que mueve a la vez métrica y variable dependiente (§Rango dinámico).
- [x] **Criterio de cierre, cumplido el 2026-09-03:** H1 respondida con su número, su figura y su párrafo. Fase B cerrada; la tanda 2 (C y E en una sesión, D, F y G en la otra) puede abrirse sobre `results/`.
- [x] **Revisión de la tarde del 2026-09-03 (flujo de memoria, con permiso de Lai sobre los ficheros del flujo de análisis), hecha ese mismo día:** diez desviaciones ([[2 - Decisiones]]); suavizado corregido en `train.median3` y campos suavizados recalculados en `efficiency.py`; `contrast.py` con jackknife, `D_ref`, granulada estratificada, hitos, `log_lr`, poda de NGV y `selection_regret`, sin `bh`; tabla larga regenerada; `signos-h1` con intervalos; §Protocolo de análisis reescrito; §Correlaciones por condición reescrita con los intervalos.

### Fase C: OE2, valor incremental (el decisivo)

- [ ] La métrica contra su referencia nombrada de antemano (val *accuracy* para VD4, val *loss* para el *gap*), celda a celda, con TSE y la otra lectura al lado y `D_ref` como redundancia; la lectura de selección de VD4 (`selection_regret`) en puntos de *accuracy* frente a la validación al 5 % y al azar; secundaria, la misma comparación sobre la cuenta granulada. «El mejor de tres» y la parcial de rangos se retiraron el 2026-09-03. La asimetría de §Problemas conocidos se declara por escrito y H2 se decide sobre *accuracy* de test y *gap*.
- [ ] El eje de coste tiene sección propia, §Coste y capacidad predictiva; aquí entra solo en cuanto OE2 pregunta si ese coste está justificado.
- [ ] **Criterio de cierre:** H2 respondida sobre las seis VD, con la limitación de la velocidad declarada, no omitida.

### Fase D: OE3, comparación de familias

- [ ] La poda está hecha (fase B, 2026-09-03, completada esa tarde) y aquí se documenta **como resultado propio**: demostrar que dos métricas de familias distintas son la misma cantidad es un hallazgo, no limpieza; que NGV es la escala de ruido con menos muestras y que la disparidad es variabilidad, también. Quedan tres por familia.
- [ ] **Reformulado el 2026-09-03:** H3 es un ranking de métricas individuales con la familia como etiqueta, y la conclusión sobre familias es cualitativa, porque la variabilidad puede quedar en una sola cantidad y comparar cuatro contra una no es lo que promete el objetivo.
- [ ] **Criterio de cierre:** H3 respondida sobre la lista podada, y la poda documentada como resultado propio.

### Fase E: OE4, suficiencia temprana

- [ ] Barrido de ventanas 5/10/25/50 %, con el 100 % como ancla: D al 5 % contra D al 50 % celda a celda, con sus intervalos, sobre las tres variables del final; en velocidad, la D por hitos entre las *epochs* 1 y 2 (MNIST) o 2 y 4 (resto), declarado en *epochs* absolutas.
- [ ] **Trampa, ya medida el 2026-09-02:** comparar ventanas para la velocidad está contaminado. VD1 solo anticipa algo al 5 % (22 celdas) y al 10 % (12 celdas); al 25 % y al 50 % describe el pasado en las 24. VD3 aguanta hasta el 10 % en 17 celdas y VD2 hasta el 25 % en todas. VD4, VD5 y VD6 se miden al final del run y no tienen ese problema, así que OE4 es limpio sobre esas tres y sobre la velocidad se restringe a las ventanas y celdas que deja el mapa de `efficiency.py::window_overlap`.
- [ ] **Criterio de cierre:** H4 respondida, con la ventana mínima defendible dicha explícitamente.

### Fase F: OE5, robustez entre optimizadores

- [ ] Diferencia pareada D con SGD menos D con Adam sobre los 12 pares de celdas que solo difieren en el optimizador, con su intervalo; una inversión solo cuando las dos D excluyen el cero y difieren de signo.
- [ ] **Asimetría medida entre los dos brazos:** los 41 runs divergidos son todos de SGD, y Adam no divergió ni una vez en sus 480. Tenerlo delante al interpretar cualquier diferencia.
- [ ] **Criterio de cierre:** H5 respondida, declarando que 12 pares dan poca potencia, que solo se detectan inversiones groseras y que un no rechazo no prueba invariancia.

### Fase G: OE6, concordancia con la literatura

- [ ] Contraste del signo observado contra el predicho por cada paper, a una cola y solo sobre las celdas cuyo intervalo excluye el cero, **solo con los signos que la tabla marca como fuertes**; los extrapolados se reportan sin contrastar, porque son extrapolación nuestra y no predicción del artículo. La tabla de signos ya está verificada contra los PDFs (2026-07-17, [[Datos experimentales]] §5.3) y la salvedad de signo de GWA está en `fundamentos.tex:168`. **No calcula nada nuevo**, consume la salida de la fase B.
- [ ] **Criterio de cierre:** H6 respondida, separando los signos que el paper afirma de los que esta memoria extrapola.

### Fase H: cierre de la memoria

- [ ] **Conclusiones**, una por objetivo.
- [ ] **Comprimir Resultados:** las tres figuras retiradas el 2026-09-03 (`solape-bandas`, `solape-cruces`, `val-test`) no vuelven, y §Concordancia y §Rango dinámico ya se recortaron esa noche; la proporción del capítulo se juzga cuando existan las secciones de las fases C a G.
- [ ] **Cerrar las promesas colgantes** listadas en §Estado actual.
- [ ] **Declarar el encuadre del análisis:** posterior a los datos (matriz terminada el 2026-08-22, plan retirado el 2026-08-25) y anterior a cualquier coeficiente (método fijado el 2026-09-03), con Nosek et al. 2018 como marco de la distinción entre predicción y postdicción y Willroth y Atherton 2024 para reportar las desviaciones con su fecha.
- [ ] **Notas de honestidad:** Tiny-ImageNet usa su val público como test, y el split es fijo y compartido por todos los runs. La de F1-macro ≈ accuracy ya está medida y escrita en `resultados.tex` §Concordancia entre validación y test (2026-09-02); solo hay que citarla.

### Fase I: obligatorio para el depósito (independiente, en cualquier hueco)

No depende de ningún resultado, así que puede adelantarse desde ya. Lo que es escritura de capítulos se ha movido a la fase 0; aquí queda solo lo administrativo y lo que solo se puede hacer al final.

- [x] **Anexo ODS, escrito el 2026-09-03** en `anexo-ods.tex`, con la tabla de los 17 y una reflexión de unas 900 palabras: 7, 9 y 12 en medio, 4 y 13 en bajo, los 12 restantes no proceden, sin ninguna cifra de energía porque solo se midieron horas de GPU. El fichero suelto para Ebrón es `thesis/anexo-ods.docx`, generado desde el `.tex` por `thesis/anexo-ods-docx.py` sobre la plantilla oficial de `docs/research/writing/`; si el `.tex` cambia, se vuelve a ejecutar.
- [ ] **Los tres resúmenes** de `main.tex` y los agradecimientos. El **anexo de configuración y reproducibilidad** está escrito desde el 2026-09-03 salvo la línea de la GPU: la memoria lleva dos marcas visibles «[pendiente: ... nvidia-smi]», una en el anexo y otra en `desarrollo.tex` §Cálculo de las métricas, que Lai rellena copiando el modelo y la memoria exactos de `nvidia-smi` en la máquina de Linux (la RTX 3080 de sobremesa salió con 10 o 12 GB, y la cifra de 16 GB que circulaba por el vault no casa).
- [ ] Formato UPV/ETSINF, Turnitin y entrega por EBRON.
- [ ] Borrador al tutor → incorporar feedback.
- [ ] Slides de defensa (10-15, ~15 min).
