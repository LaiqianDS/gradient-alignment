## Estado actual (rev. 2026-09-04)

Aquí viven el estado y el plan. El *qué se decidió y por qué* está en [[2 - Decisiones]] y el *estado resultante del diseño* en [[1 - Diseño]]; este documento no los repite, los referencia. Tampoco guarda el camino recorrido, que ya está en el historial de git, solo dónde está el trabajo hoy. **Las fases 0, A, B, C, D, E, F y G están cerradas** y lo siguiente es la fase H, el cierre de la memoria.

- **Matriz: TERMINADA.** 960 de 960 runs, las 24 celdas con sus 40 cada una, versionados en git desde el commit `f6df900` (2026-08-22), así que existen fuera de esta máquina. Coste real 121,7 h de reloj, 97,6 h de entrenamiento y 24,1 h de instrumentación.
- **Siguiente paso: la fase H**, el cierre de la memoria, con los seis veredictos delante: H1 se sostiene como puerta, H2 cae, H3 cae, H4 se sostiene porque las métricas no maduran, H5 no se refuta ni queda demostrada, H6 cae en parte (revisión del 2026-09-04). Las Conclusiones, una por objetivo; comprimir Resultados; recontar las promesas colgantes; declarar el encuadre. La fase I es independiente y cabe en cualquier hueco.
- **El método de análisis se fijó el 2026-09-03 por la mañana, se ejecutó por la tarde y se revisó esa misma tarde en diez puntos** tras la revisión externa de la memoria, después de la primera lectura de H1 y por motivos de estructura ([[2 - Decisiones]], entrada «Diez desviaciones»; resumen en [[1 - Diseño]] §Método de análisis). Como el plan preregistrado se retiró, el análisis es **posterior a los datos**, el método de la mañana es **anterior a cualquier coeficiente**, y las desviaciones se declaran con fecha y motivo; así está presentado en `metodologia.tex` §Cuándo se fijó, con las tres fechas.
- **La tabla larga es la entrada de todo lo que queda**: `results/tabla_larga.parquet` (los 806 que aprendieron) y `results/tabla_larga_960.parquet` (sensibilidad, solo en `results/`), una fila por celda, ventana, predictor y variable, con la D, sus pares y su error típico por jackknife, `D_ref` contra la referencia nombrada, la granulada estratificada con sus pares, la D por hitos con el número en riesgo para la velocidad y la fracción por delante; doce predictores, los once titulares más `log_lr`. `results/seleccion.parquet` trae la lectura de selección por celda y predictor, con el signo del artículo (`regret`) y el invertido (`regret_flipped`); `results/incremental.parquet` es la lectura de H2 por ventana, variable y predictor; `results/ventanas.parquet` y `ventanas_recuento.parquet` son la lectura de H4, |D| al 50 % menos |D| al 5 % por celda con jackknife pareado (velocidad por hitos, epoch 1 frente a 2 y 2 frente a 4). Desde el 2026-09-04 la tabla larga lleva además `D_diff` y `se_diff` (|D| de la métrica menos |D| de su referencia, con jackknife pareado) y sus `_land`. `results/ranking.parquet` y `ranking_grupos.parquet` son la lectura de H3, el orden de las métricas por celdas del signo mayoritario, sobre las 24 celdas y dentro de cada conjunto y arquitectura; `results/optimizadores.parquet` es la de H5 sobre los 12 pares de celdas que solo difieren en el optimizador; `results/signos.parquet` es la de H6, celdas a favor y en contra del signo que cada artículo predice, sobre las 24 y dentro de cada arquitectura. Los diez se regeneran con `uv run python src/contrast.py` (unos 2 min) y son del 2026-09-04.
- **Código.** `src/analysis.py` es el backend de sanidad de las columnas y tiene el rango dinámico del punto 3. `src/efficiency.py` tiene por unidad el run y la celda: censo de salud (`run_health`), mapa de lo computable (`vd_status`), solape (`window_overlap`), concordancia validación-test, recuento de formas (`shape_census`, `declared_cells`), los tres campos suavizados recalculados desde `trajectory.parquet` (`smoothed_fields`) y la D con su jackknife y sus estratos (`d_stats`, `somers_d`). `src/contrast.py` es la máquina del contraste: `outcomes`, `long_table`, `primary_family` (hitos para la velocidad), `sign_counts`, `incremental_table` (H2: gana/pierde, intervalos de la diferencia, redundancia con `REDUNDANT_D`, selección), `selection_regret`, `flipped_ends`, `window_table` y `window_counts` (H4), `ranking_table` (H3, con `FAMILY` como etiqueta de familia, que `figures.py` importa), `optimizer_table` (H5), `concordance_table` sobre `PREDICTED` (H6; la tabla de signos del vault por variable, atada a `GOOD_END` por una prueba), probada sobre directorios sintéticos de efecto conocido (`tests/test_contrast.py`); `efficiency.d_diff_stats` es la diferencia pareada con su jackknife. `src/figures.py` tiene una función por figura, diez funciones de las que la memoria usa **siete** (`ventana-lr`, `rango-celda`, `rango-columnas`, `solape-celda`, `signos-h1`, `seleccion`, `ventanas`); `solape-cruces`, `solape-bandas` y `val-test` siguen en el fichero y fuera de la memoria. Estilo de artículo en `figstyle.py` (Okabe-Ito, TeX Gyre Heros) y rótulos en castellano. Los tamaños de las particiones viven en `config.py::VAL_SIZE` y `TEST_SIZE`. Suite en **314** pruebas verdes.
- **Runs con NaN.** 154 nunca aprendieron (115 por colapso, 39 por divergencia) y 11 se recuperaron; los recuentos completos están en [[2 - Decisiones]] (2026-08-31). El cero falso de `stiffness/sign_*` y `confusion/frac_neg` está parcheado en `reports/` desde el 2026-08-29, **así que se comporta como cualquier valor ausente y ninguna fase posterior necesita una excepción**.
- **Qué hay realmente registrado por época.** `trajectory.parquet` trae **23 columnas de métrica de gradiente** (no 8: cada métrica emite varias claves) más **cuatro columnas de TSE**, más la curva cruda `train_loss`/`val_loss`/`val_acc` y los dos relojes. `metrics_at_window.parquet` son esas mismas columnas en cinco filas por run, una por ventana.
- **Memoria.** Escritos y corregidos con la revisión externa del 2026-09-03 (`docs/research/writing/Revisión externa de la memoria.md`, gitignorada; el detalle de cada corrección está en el commit `86b3880`): Introducción, Estado del arte, Fundamentos, Metodología entera con §Protocolo de análisis y trece riesgos, Desarrollo, Trabajos futuros, el anexo ODS y el anexo de configuración. De Resultados están las cuatro secciones de la fase A, §Correlaciones por condición (H1), §Valor incremental (H2, cae), §Coste y capacidad predictiva, §Análisis agregado y barrido temporal (H4, se sostiene porque las métricas no maduran) §Qué familia predice mejor (H3, cae), §Robustez entre optimizadores (H5, no se refuta ni queda demostrada) y §Concordancia con la literatura (H6, cae en parte); la hoja de ruta y las conclusiones del capítulo son títulos sin prosa, igual que Conclusiones. Faltan también los tres resúmenes de `main.tex`, los agradecimientos, el diagrama del *pipeline* de Desarrollo y la línea de la GPU, con dos marcas «[pendiente: ... nvidia-smi]» en el anexo y en Desarrollo. **Siete figuras** en el cuerpo. Compila en 104 páginas sin referencias ni citas sin resolver ni cajas desbordadas (96 antes de la fase C, 106 antes de la revisión del 2026-09-04 y del recorte de Fundamentos de la otra sesión); la nota del seminario recomienda entre 50 y 100, faltan unas 3 por escribir (Conclusiones y los resúmenes), y si hay que recortar más los candidatos son §Configuración del entrenamiento (las referencias de accuracy y la cota de GenURL), las lecturas H3 a H6 de §Protocolo y Fundamentos.
- **Promesas colgantes.** LaTeX no detecta ninguna, porque un `\ref` resuelve igual aunque la sección esté vacía, así que «cero referencias indefinidas» no vale como comprobación y hay que contarlas a mano, comparando cada `\label` con la prosa que le sigue; escribir una sección sube tantas como baja, porque cada remisión honesta abre una nueva. Último recuento, 2026-09-03: cuatro, hacia Resultados y Conclusiones, que cierran las fases C a H; las remisiones de falsación de Introducción, Estado del arte y Fundamentos son anuncios, no errores. Se recuentan en la fase H.
- **Pilot:** ejecutado y leído; los presupuestos que salieron de él están en `config.py::DATASET_BUDGET` y en los 24 YAML. Los umbrales viven en `config.py::THRESHOLD_ACC`, calculados sobre la matriz desde el 2026-09-01. `reports_pilot/` está versionado desde el 2026-08-29, con los seis `testfix_40ep/` de Tiny dentro.

## Vigentes: a tener en cuenta en todo lo que queda de análisis (rev. 2026-09-03)

Lista de comprobación para abrir cualquier sesión de análisis. Entra aquí lo que **condiciona un cálculo futuro** y se olvidaría con facilidad: trampas medidas, equivalencias entre columnas, y decisiones abiertas que cambian un número. No entra el estado del trabajo, que está arriba, ni el porqué de cada cosa, que está en [[2 - Decisiones]]. Una línea por asunto y un puntero. Cuando algo deja de condicionar, se borra de aquí.

- **El método es posterior a los datos.** La matriz acabó el 2026-08-22 y el plan se retiró el 2026-08-25. Ninguna frase, en el vault o en la memoria, puede sugerir preinscripción.
- **El umbral de VD1 es por dataset y arquitectura desde el 2026-09-01** ([[2 - Decisiones]]), y sale de una norma reproducible: el valor redondo más alto que alcanza al menos el 60 % de los entrenamientos que aprendieron. Si se recalcula, se recalcula con la norma y no a mano. Y **VD1 se recalcula siempre en la capa de análisis** desde `trajectory.parquet`: el campo `epochs_to_threshold` de los 960 `summary.json` está calculado con los umbrales viejos y **no se puede leer**. Desde la tarde del 2026-09-03 lo mismo vale para `best_val_acc` y `best_val_loss`: el suavizado corregido (valor crudo en la primera y la última *epoch*) se aplica en `efficiency.py` y los tres campos suavizados de `summary.json` son obsoletos.
- **El learning rate mueve el predictor y también la variable a predecir**, medido el 2026-09-02. Un reparto de 0,90 dice que nueve décimas de esa columna son el learning rate leído de otra manera, así que la correlación de la fase B será en buena parte una correlación entre dos efectos de una causa común. Declarado en `resultados.tex` §Rango dinámico del predictor, y hay que volver a decirlo al interpretar cada contraste.
- **El predictor de referencia gana en rango dinámico a todas las métricas caras**, TSE 0,97 y la curva de validación 0,95 frente a 0,92 la mejor instrumentada. Es un mal presagio para H2 y hay que tenerlo delante al escribir la fase C.
- **VD1 y el predictor de referencia leen la misma curva.** Descontar la validación temprana para predecir VD1 es descontar un prefijo del propio resultado, y en un run ya cruzado la validación leída en la ventana vale ya el umbral. H2 se decide sobre VD4 y VD5, y hay que repetirlo al redactar OE2 y OE4.
- **El solape está medido por celda desde el 2026-09-02, en pares, y acota OE4 sobre la velocidad.** VD1 conserva la mitad por delante en la ventana del 5 % en 22 celdas y en la del 10 % en 12, y en ninguna al 25 % ni al 50 %. VD3 da 24, 17, 9 y 2 celdas con el suavizado corregido, y donde falla es por sobreajuste, no por velocidad. VD2 sirve hasta el 25 % en las 24 y al 50 % cae sobre la línea por construcción, «descrita a medias». **La cuenta es un suelo**, porque no haber cruzado no es estar por decidir, y la regla de la mitad (`efficiency.py::AHEAD_FLOOR`) describe y no decide desde el 2026-09-03: la lectura primaria de la velocidad es **por hitos**, la D solo entre los runs que aún no habían cruzado al cerrar la ventana, censurados incluidos, con el número en riesgo al lado; la D sobre todos los runs se publica como secundaria. Los recuentos por celda viven en la prosa de §Solape, sin figura.
- **GWA va al revés que su artículo.** Aquí se mide sobre $\nabla\ell$ bruto, así que la predicción heredada es que una GWA **baja** acompañe a mejor generalización. Conversión en `fundamentos.tex` §Métricas de alineación, con el mismo aviso en el enunciado de H6 de [[1 - Diseño]] y en la tabla de signos de [[Datos experimentales]] §5.3, que es el material contra el que se contrasta H6.
- **En los 41 runs divergidos, `final_test_acc` y `final_gap_acc` traen número y no son mediciones**, porque `argmax` sobre logits NaN devuelve siempre el índice 0. Son las dos únicas columnas donde "no falta ningún valor" engaña. Los 41 son todos de SGD: Adam no divergió nunca.
- **`noise_scale/simple` y `mcoh/global` son la misma cantidad** (GNS = M/α − 1 exacto, Spearman intra-run −1,000), y GSNR es el recíproco por parámetro de NGV. Desde el 2026-09-03 la m-coherencia sale del contraste y la escala de ruido la representa; la predicción de signo de su artículo (H6) se lee sobre la escala con el signo cambiado. NGV y GSNR no son redundantes entre sí, |D| mediana 0,59 al 5 %, pero **NGV sale también desde la tarde del 2026-09-03**, porque es la escala de ruido estimada con diez submuestras de 25 (Spearman 0,94 dentro de celda con la escala de ruido) y la regla de la mañana nunca la comparó con ella. La *gradient disparity* pasa a variabilidad, porque es √(2·tr Σ/51). Quedan seis métricas de gradiente, tres por familia.
- **En velocidad, la familia primaria lee las 24 celdas por hitos** (`contrast.primary_family` toma `D_land` para VD1), con el número en riesgo declarado; `AHEAD_FLOOR` ya no filtra celdas (2026-09-03, tarde). Donde quedan pocos en riesgo, el intervalo lo dice.
- **`results/` guarda tablas derivadas y versionadas**, nunca datos crudos, y cada fichero se regenera con una orden desde `reports/`; los números de la memoria que salgan de ahí se citan con la fecha del fichero.
- **Cada métrica entra por su columna titular**, fijada el 2026-09-03 en la tabla de `metodologia.tex` §Variables y en `analysis.py::headline_columns()`. Las demás columnas de las 27 no se contrastan, o solo como exploratorio.
- **El estadístico de todos los contrastes es la D de Somers sobre pares comparables** (2026-09-03): valores distintos de la VD y, en velocidad, al menos un cruce; un cruce en la última *epoch* contra un censurado es comparable. Cada D lleva su error típico por jackknife (quitar un run) y un intervalo normal al 95 %; una celda muestra asociación si el intervalo excluye el cero. Agregación: cuántas celdas la muestran y con qué signo, con desglose por conjunto; el recuento de signos es descripción y la referencia binomial (18 de 24, 10 de 12) solo una vara; sin Benjamini-Hochberg (2026-09-03, tarde).
- **H2 cayó el 2026-09-04** ([[2 - Decisiones]]): ninguna métrica supera a su referencia con intervalo, ninguna es redundante con ella (|D_ref| mediana 0,23 a 0,45) y todas pierden más accuracy que la validación al elegir el learning rate (GWA 0,023 frente a 0,013). Las fases D a G y las Conclusiones se escriben con ese veredicto delante: lo que las métricas ordenan es el learning rate, y la posición en la rejilla empata con la mejor.
- **H3 cayó el 2026-09-04** ([[2 - Decisiones]]): ninguna familia domina; GWA y la stiffness encabezan las tres variables y la confusión y GWA las cierran; el ganador cambia con la arquitectura (red plana alineación 7/8, convolucional variabilidad) y GWA invierte su signo con el gap entre la convolucional y la ResNet-18. Las Conclusiones y la portada se escriben con eso delante: la afirmación es sobre dos métricas, no sobre una familia.
- **H5 no se refutó ni quedó demostrada el 2026-09-04** ([[2 - Decisiones]]): 0 a 2 inversiones de 12 por métrica, de dos en dos con las dos variables de un mismo par (disparidad en cifar10-fc, stiffness en cifar100-resnet18); lo que cambia con el optimizador es el tamaño de la D. «Invariante» no se escribe en ningún sitio; se escribe «no se ha visto lo contrario».
- **H6 cae en parte desde la revisión del 2026-09-04** ([[2 - Decisiones]], dos entradas de esa fecha): de siete predicciones, dos se cumplen (GWA con test 15/1, GSNR con gap 11/2, en las tres arquitecturas), tres no reciben apoyo (confusión y escala de ruido con la velocidad, m-coherencia con el gap) y dos se invierten (stiffness con la velocidad 9 frente a 5, disparidad con el accuracy de test 12 frente a 4), las dos por composición de curvas sobre el learning rate. Las Conclusiones lo escriben así: concordar con el artículo no es superar a la validación, y «no superan» no es «no añaden».
- **El jackknife por run es ancho, no falso** (revisión del 2026-09-04): los ocho learning rates de una seed comparten inicialización, orden y probe, y el learning rate es fijo; quitar una seed entera da errores típicos 0,58 veces menores y más celdas seguras. Se mantiene el de run como intervalo único; `log_lr` no tiene varianza de seed, así que la puerta de H1 se lee también por signo mayoritario.
- **La posición en la rejilla, `log_lr`, es un predictor de coste cero en todas las tablas** (2026-09-03, tarde): el techo de cualquier lectura monótona del *learning rate*; una métrica que no lo supera es el *learning rate* leído de otra forma.
- **H2 se compara con una referencia nombrada de antemano**, el *accuracy* de validación para VD4 y el *loss* de validación para el *gap*, y `D_ref` mide dentro de la celda si la métrica es la referencia con otro nombre; la lectura de selección (`contrast.selection_regret`) da el *accuracy* de test que cuesta elegir el *learning rate* con la métrica al 5 % frente a elegirlo con la validación al 5 % (2026-09-03, tarde).
- **Ninguna desviación posterior a la tabla larga se justifica por un coeficiente.** La cronología (método por la mañana, tabla y primera lectura de H1 por la tarde, revisión externa sin correlaciones, desviaciones después) está en [[2 - Decisiones]] y se declara en §Cuándo se fijó.
- **Las variables dependientes y los tres predictores gratuitos hacen valle o pico a lo largo del *learning rate*, y las métricas de gradiente son irregulares** (2026-09-03, tolerancia del 5 % del recorrido). Las celdas donde una métrica monótona no puede dar relación monótona, de 1 a 7 por métrica, se declaran antes de calcular.
- **La *seed* no mueve las métricas de alineación** (0,03 a 0,08 frente a 0,13 de referencia al 5 %) **y mueve algo las de variabilidad** (0,17 y 0,12). La cuenta granulada solo tiene con qué trabajar en variabilidad; predicción registrada antes de calcularla. Desde la tarde del 2026-09-03 es estratificada (los pares dentro de *learning rate* sumados) y lleva su número de pares.
- **Empates en la velocidad:** mediana del 13 % de los pares entre cruces por celda, máximo 44 %; los pares empatados no son comparables.
- **TSE es una suma acumulada**, no el *loss* de la *epoch*: a ventana fija ordena como la media del *loss* de las primeras *epochs*.
- **154 runs nunca aprendieron y los 154 tienen causa conocida**, 115 de colapso y 39 de divergencia. Se identifican por `learned` de `efficiency.py::run_health`: mejor val-accuracy suavizada por debajo de **1,25 veces el azar** (`CHANCE_MARGIN`); 1,2 y 1,25 seleccionan los mismos 154. **Distinguir dos usos, que no son el mismo:** se excluyen del denominador que **calibra** el umbral de VD1 (norma del 2026-09-01), porque no cruzarían ninguno; desde el 2026-09-03 la población primaria de todo **contraste** son los 806 que aprendieron y los 960 son la sensibilidad declarada.
- **La censura de VD1 se cuenta en pares, no en runs.** Un run censurado sigue siendo comparable con cualquiera que cruzó. 15 cruces de 40 conservan el 61,5 % de los pares, no el 37,5 %. Con n = 40 en las 24 celdas, esa fracción es una función estrictamente creciente del número de cruces, así que da la escala honesta y **no una evidencia aparte**, porque los dos números dicen lo mismo.
- **Los 24 YAML de `experiments/` llevan el umbral viejo por conjunto de datos.** `run_matrix.py --init` no pisa ficheros existentes, así que siguen ahí desde que se corrió la matriz. No afecta a ningún cálculo, porque el análisis lee `config.py::THRESHOLD_ACC` y nunca los YAML, pero engaña a quien los abra.
- **El suelo de ajuste del gap está fijado desde el 2026-09-03:** entran en VD5 y VD6 los runs cuya `final_train_eval_acc` alcanza el τ de su celda. Es un filtro de análisis, no obliga a re-correr nada, y deja 693 de los 806, con 25 por celda de mediana, 40 en cinco celdas y 20 en tres.

## Plan por objetivos (rev. 2026-09-03; el objetivo de la primera semana de septiembre de 2026 queda superado y no hay fecha nueva: Lai fijó el 2026-09-03 que se termina lo antes posible)

**Metodología de trabajo, fijada el 2026-08-27: una cosa cada vez, y en tres carriles a la vez.** Son dos mitades que se sostienen la una a la otra, y gobiernan todo lo que queda del proyecto.

*Una cosa cada vez.* En cada momento hay un solo tema abierto, y no se abre otro hasta cerrarlo. Lo que aparezca por el camino se anota donde corresponda y se sigue con lo que había. Dispersarse deja tres cosas al setenta por ciento, y tres cosas al setenta por ciento valen cero.

*Tres carriles a la vez.* Ese tema único avanza en los tres sitios en la misma tanda: el **código** de `src/` y `tests/`, la **documentación** del vault, y la **memoria** en `thesis/`. No se adelanta el código para escribir el texto más tarde, porque el texto que se deja para más tarde no se escribe. Si un paso no toca alguno de los tres carriles, se dice por qué en vez de dejarlo en silencio.

**La regla de fase, y no se negocia: una fase, un objetivo, un entregable completo.** Una fase se cierra cuando su código está escrito y probado, sus números calculados sobre `reports/`, su figura hecha, y su texto redactado en el `.tex` correspondiente. Nada pasa a la fase siguiente con algo a medias, porque lo que queda a medias es lo que no se hace.

**Segunda regla, del 2026-08-26:** toda cuenta que se ejecute tiene que poder responder de qué objetivo es y qué se haría distinto si saliera al revés. Si no hay respuesta, no se ejecuta. Y el contraste no puede ser solo visual: cada objetivo lleva su prueba de hipótesis, que hay que reconstruir porque la anterior se retiró.

**Regla de figuras, del 2026-08-26:** una figura, una afirmación, con la evidencia numérica visible. Si no se puede resumir en una frase que empiece por "esta figura demuestra que", está mostrando datos y no un resultado, y su sitio es un anexo o el repositorio.

**Regla de capítulo, del 2026-08-27:** la plantilla de [[Estilo de redacción - notas del TFM HOFT]] rige en los capítulos de cuerpo, del 2 al 6, y no en la Introducción, las Conclusiones ni Trabajos futuros, que cierran solos. La hoja de ruta y las conclusiones de un capítulo se escriben junto a su cuerpo y **nunca antes**, para no dejar un título sin prosa. Solo falta la de Resultados, y la instrucción está como comentario en ese fichero.

**Regla de registro, del 2026-08-29:** el vault se poda mientras se trabaja, no al final. Una entrada de [[2 - Decisiones]] guarda cuatro cosas y ninguna más: qué se decidió, por qué en la forma más corta que aún lo defienda ante un tribunal, la evidencia medida con su número, y las trampas que le costarían tiempo a quien lo toque después. Se va todo lo demás: el camino hasta la decisión, las alternativas que ya no informan nada, el detalle de la verificación, y lo que el código o la memoria ya cuentan mejor. **Lo que no se poda nunca son las decisiones y sus fechas**, porque son la evidencia del encuadre que la fase H tiene que declarar, que el análisis es posterior a los datos y anterior a los resultados.

**Esto es estructura, no compromiso.** Lo único fijo son las reglas de arriba. Todo lo que sigue es un esqueleto para no perderse, y **cada fase puede cambiar**: su método, su contenido, sus entregables, su orden, y una fase puede partirse en dos o desaparecer. Cada fase se abre decidiendo qué se hace en ella y registrando esa decisión en [[2 - Decisiones]] antes de programar nada. Si al abrir una fase resulta que las siguientes ya no tienen sentido tal como están escritas aquí, se reescriben aquí y se sigue: este documento va detrás del trabajo, no delante.

### Fase 0: poner el LaTeX al día

**Cerrada el 2026-08-28.** Puso la memoria al día con todo lo que ya se sabía, sin depender de ningún resultado: plantilla de capítulo, estilo de figuras, terminología en inglés, los capítulos de Metodología, Desarrollo y Trabajos futuros, y la revisión de Lai sobre el texto. Cada decisión está en [[2 - Decisiones]], y lo que sigue vivo de esta fase está arriba, en §Estado actual y en las reglas.

### Fase A: datos y validez

**Cerrada el 2026-09-03.** No atacó ningún objetivo: estableció que los datos sirven y qué se puede calcular con ellos, un lado cada vez, predictores y variables dependientes por separado, porque los puntos 3 y 4 descartan dos de las tres causas de un coeficiente nulo antes de mirar ninguna correlación. Puntos 1 y 2 (del 2026-08-29 al 2026-09-01): columnas válidas sobre los 960 y un cero falso parcheado en `reports/`, censo de runs y mapa de lo computable, umbral por conjunto de datos y arquitectura, figura `ventana-lr`, §Qué variables quedan medidas. Punto 3 (2026-09-02): rango dinámico, §Rango dinámico del predictor, figuras `rango-celda` y `rango-columnas`. Punto 4 (2026-09-02): solape en pares, §Solape entre la ventana y el desenlace, figura `solape-celda`. Punto 5 (2026-09-02): concordancia validación-test, §Concordancia entre validación y test, sin figura desde el 2026-09-03. Decisión de las nubes (2026-09-03), cerrada sin mirar ninguna: la forma se lee lado a lado a lo largo del *learning rate*, y el recuento entró en `efficiency.py` con la fase B. El método de cada punto está en [[2 - Decisiones]] con su fecha. **Lección del carril de LaTeX:** los números del vault se vuelven a medir sobre `reports/` antes de entrar en la memoria, no se copian; tres salieron mal así.

**Las fases B a G son una máquina y seis consultas (visto el 2026-08-31, máquina fijada el 2026-09-03).** No son seis trabajos. La fase B construye el contraste entero y lo estrena con OE1; las otras cinco añaden una idea cada una encima de la misma maquinaria, y OE6 no calcula nada nuevo, solo lee signos. Las ocho elecciones de esa máquina están decididas y con base en [[2 - Decisiones]] (2026-09-03) y resumidas en [[1 - Diseño]] §Método de análisis. La poda de métricas redundantes va antes de la fase B, porque decide qué columnas entran como predictor en *todos* los objetivos.

**Tandas y flujos (2026-09-03).** Tanda 1, cerrada: la fase B y la revisión de la memoria. Tanda 2, abierta: C y E en una sesión y D, F y G en la otra, o las cinco en una, según decida Lai; todas leen `results/` y escriben solo su sección de Resultados, porque §Protocolo de análisis ya está escrito para los seis objetivos. Tanda 3, en serie: H y el resto de la I. Reparto de ficheros en [[2 - Decisiones]].

Entrada común a las cinco: `results/tabla_larga.parquet`, con `contrast.primary_family` para la familia primaria, y `results/seleccion.parquet`. Ninguna lee `summary.json` directamente.

### Fase B: OE1, existencia

**Cerrada el 2026-09-03.** Construyó la máquina del contraste y la estrenó con H1. Paso 1, la poda y el recuento de formas en `efficiency.py`; paso 2, `src/contrast.py` con pruebas sobre datos sintéticos de efecto conocido antes de tocar `reports/`, porque un contraste no validado contra un efecto que se sabe que existe no distingue «no hay señal» de «tengo un bug»; paso 3, §Protocolo de análisis entero para los seis objetivos; paso 4, §Correlaciones por condición con la tabla de recuentos y la figura `signos-h1`. Esa misma tarde la revisión externa desvió el método en diez puntos ([[2 - Decisiones]]) y los cuatro pasos se rehicieron con ellos. **Resultado vigente:** H1 se sostiene como puerta, porque GWA muestra asociación con la velocidad en 16 celdas y con el *accuracy* de test en 15, y la *gradient disparity* con el *accuracy* de test en 12, más celdas que la posición en la rejilla y con el mismo signo en los cuatro conjuntos; la *stiffness* con el *gap* iguala a la posición en la rejilla, 15 celdas; la cuenta granulada no pasa de tres celdas en ninguna de las 30 combinaciones, así que la asociación es la del *learning rate*. Aviso para H2: GWA ordena las celdas como el *accuracy* de validación, `D_ref` de mediana −0,45, y la *stiffness* −0,44.

**Plan de las cinco fases, fijado el 2026-09-04.** Orden C, E, D, F, G: primero las dos que calculan algo nuevo, luego las tres que solo leen. Cada fase va en cuatro pasos, uno por turno y con la puerta de Lai en cada uno: la explicación de la lectura en el formato aprobado, los números, la tabla o la figura, y el texto en `resultados.tex` más el vault. Casi todo son tablas con el mejor en negrita, por la regla de HOFT de que una comparación de niveles va a tabla, y solo dos figuras, la pérdida por selección de C y la curva de ventanas de E, así que Resultados crece poco. Lo que tenemos es lo mismo para las cinco: `results/tabla_larga.parquet`, 8.640 filas, una por celda, ventana, predictor y variable, con la D, su error típico, `D_ref`, la granulada, la fracción por delante y la D por hitos con su número en riesgo; `contrast.primary_family`, 720 filas de la familia primaria; y `results/seleccion.parquet`, 216 filas, una por celda y predictor, con la pérdida por selección y la del azar.

### Fase C: OE2, valor incremental (el decisivo)

**Cerrada el 2026-09-04.** Tres reglas fijadas antes de calcular (intervalo de la diferencia pareada, redundancia en 0,8, selección por mediana y por celda con el signo del artículo; [[2 - Decisiones]]), código con prueba (`d_diff_stats`, `incremental_table`, `flipped_ends`, columnas `D_diff`), `results/incremental.parquet` y `seleccion.parquet` regenerados, figura `seleccion`, y §Valor incremental y §Coste y capacidad predictiva escritas. **Resultado: H2 cae.** Sobre el accuracy de test la mejor métrica, GWA, gana 11 y pierde 13 en crudo y 0 y 8 con intervalo, igual que la posición en la rejilla; sobre el gap la referencia tiene |D| mediana 0,54 frente a 0,21 a 0,39 de las métricas; en la selección la validación pierde 0,013 y GWA 0,023, con las otras cinco entre 0,035 y 0,110 y tres peores que el azar con el signo de su artículo. La velocidad se calculó y se publicó sin decidir. Trampas y cifras completas en la entrada del 2026-09-04.

### Fase E: OE4, suficiencia temprana

**Cerrada el 2026-09-04.** Comparación del 5 al 50 % con el jackknife pareado de C (`window_table`, `window_counts`, dos parquet), velocidad por hitos entre las dos primeras ventanas con varianzas sumadas, figura `ventanas` y §Análisis agregado y barrido temporal escrita. **Resultado: H4 se sostiene para las métricas, porque no maduran.** Ninguna crece con intervalo en más de seis celdas sobre el accuracy de test, con medianas de |D| planas entre 0,2 y 0,35, mientras la validación sube de 0,40 a 0,70; en el gap la validación pierde |D| al esperar; en velocidad casi nada se mueve con intervalo. La ventana mínima defendible es la primera. Cifras y trampas en [[2 - Decisiones]] (2026-09-04).

### Fase D: OE3, comparación de familias

**Cerrada el 2026-09-04.** Orden de las seis métricas por variable con la regla del protocolo (`contrast.ranking_table`, `results/ranking.parquet`), repetido dentro de cada conjunto y arquitectura (`ranking_grupos.parquet`), y la poda vuelta a medir con la D dentro de celda (`efficiency.pair_agreement`) y contada como resultado; tabla `tab:h3` y §Qué familia predice mejor, sin figura. **Resultado: H3 cae.** La alineación pone la primera métrica en las tres variables (GWA 16 y 15 celdas, stiffness 15) y la última en dos, y la variabilidad la segunda en las tres (disparidad 9, 12 y 11); por arquitectura la alineación gana las tres en la red plana con 7 de 8 celdas y la variabilidad dos en la convolucional, y GWA invierte su signo con el gap entre la convolucional y la ResNet-18. Poda: m-coherencia |D| = 1 en las 24, NGV 0,84 con la escala de ruido (0,59 con GSNR), disparidad 0,93 con la raíz de la traza. Cifras y trampas en [[2 - Decisiones]] (2026-09-04).

### Fase F: OE5, robustez entre optimizadores

**Cerrada el 2026-09-04.** Acuerdo e inversión por par solo entre D seguras, diferencia SGD menos Adam con varianzas sumadas y recuento de diferencias fuera del cero (`contrast.optimizer_table`, `results/optimizadores.parquet`); tabla `tab:h5` en §Robustez entre optimizadores, renombrada, sin figura. **Resultado: H5 no se refuta ni queda demostrada.** Ninguna métrica invierte en más de dos pares de doce (disparidad 1/2/2, stiffness 1/1/0, GNS 0/0/1, las otras tres 0), las inversiones llegan de dos en dos con las dos variables de un mismo par, y los acuerdos de las métricas son sobre 1 a 6 pares porque en el resto una D incluye el cero; lo que cambia con el optimizador es el tamaño (disparidad 0,48 → 0,23 en velocidad, stiffness 0,21 → 0,43 en gap). Las tres salvedades están escritas. Cifras y trampas en [[2 - Decisiones]] (2026-09-04).

### Fase G: OE6, concordancia con la literatura

**Cerrada el 2026-09-04.** Por predicción que el artículo enuncia, celdas a favor y en contra del signo predicho, a una cola y con el mismo intervalo de todas las tablas, sobre las 24 celdas y por arquitectura (`contrast.PREDICTED`, `concordance_table`, `results/signos.parquet`); tabla `tab:h6` en §Concordancia con la literatura, sin figura; las extrapoladas en la segunda mitad de la tabla, sin veredicto. **Resultado: H6 no se refuta.** Las dos predicciones sobre el desenlace se cumplen (GWA con test 15/1, GSNR con gap 11/2, en las tres arquitecturas), las tres sobre la velocidad no reciben apoyo (escala de ruido 7/3, confusión 5/4 con cambio de bando entre la red plana y la convolucional, stiffness 5/9 en contra, por composición de curvas: al 5 % ordena al revés que el accuracy de validación), y la m-coherencia sobre el gap da 5/6. **Revisada esa misma tarde:** la disparidad con el accuracy de test entra como predicción del artículo, porque su signo estaba en la tabla de signos y en `GOOD_END`, sale al revés (4 frente a 12) y H6 pasa a caer en parte; los nueve cambios de la revisión (H6, el párrafo del jackknife, H1, H2, H4 y cuatro frases) están en [[2 - Decisiones]] (2026-09-04, entrada de la revisión).

### Fase H: cierre de la memoria

- [ ] **Conclusiones**, una por objetivo.
- [ ] **Comprimir Resultados:** las tres figuras retiradas el 2026-09-03 (`solape-bandas`, `solape-cruces`, `val-test`) no vuelven, y §Concordancia y §Rango dinámico ya se recortaron esa noche; la proporción del capítulo se juzga ahora que existen las secciones de las fases C a G.
- [ ] **Recontar y cerrar las promesas colgantes** (§Estado actual).
- [ ] **Declarar el encuadre del análisis:** posterior a los datos (matriz terminada el 2026-08-22, plan retirado el 2026-08-25) y anterior a cualquier coeficiente (método fijado el 2026-09-03), con Nosek et al. 2018 como marco de la distinción entre predicción y postdicción y Willroth y Atherton 2024 para reportar las desviaciones con su fecha.
- [ ] **Notas de honestidad:** Tiny-ImageNet usa su val público como test, y el split es fijo y compartido por todos los runs. La de F1-macro ≈ accuracy ya está medida y escrita en `resultados.tex` §Concordancia entre validación y test (2026-09-02); solo hay que citarla.

### Fase I: obligatorio para el depósito (independiente, en cualquier hueco)

No depende de ningún resultado, así que puede adelantarse desde ya. Lo que es escritura de capítulos se ha movido a la fase 0; aquí queda solo lo administrativo y lo que solo se puede hacer al final.

- [x] **Anexo ODS, escrito el 2026-09-03** en `anexo-ods.tex`, con la tabla de los 17 y una reflexión de unas 900 palabras: 7, 9 y 12 en medio, 4 y 13 en bajo, los 12 restantes no proceden, sin ninguna cifra de energía porque solo se midieron horas de GPU. El fichero suelto para Ebrón es `thesis/anexo-ods.docx`, generado desde el `.tex` por `thesis/anexo-ods-docx.py` sobre la plantilla oficial de `docs/research/writing/`; si el `.tex` cambia, se vuelve a ejecutar.
- [ ] **Los tres resúmenes** de `main.tex` y los agradecimientos. El **anexo de configuración y reproducibilidad** está escrito desde el 2026-09-03 salvo la línea de la GPU: la memoria lleva dos marcas visibles «[pendiente: ... nvidia-smi]», una en el anexo y otra en `desarrollo.tex` §Cálculo de las métricas, que Lai rellena copiando el modelo y la memoria exactos de `nvidia-smi` en la máquina de Linux (la RTX 3080 de sobremesa salió con 10 o 12 GB, y la cifra de 16 GB que circulaba por el vault no casa).
- [ ] Formato UPV/ETSINF, Turnitin y entrega por EBRON.
- [ ] Borrador al tutor → incorporar feedback.
- [ ] Slides de defensa (10-15, ~15 min).
