## Estado actual (rev. 2026-09-06)

Aquí viven el estado y el plan. El *qué se decidió y por qué* está en [[2 - Decisiones]] y el *estado resultante del diseño* en [[1 - Diseño]]; este documento no los repite, los referencia. Tampoco guarda el camino recorrido, que ya está en el historial de git, solo dónde está el trabajo hoy. **Las fases 0 a H están cerradas** y lo que queda es la fase I, lo obligatorio para el depósito.

- **Matriz: TERMINADA.** 960 de 960 runs, las 24 celdas con sus 40 cada una, versionados en git, así que existen fuera de esta máquina. Coste real 121,7 h de reloj, 97,6 h de entrenamiento y 24,1 h de instrumentación.
- **Los seis veredictos**, con su evidencia en [[2 - Decisiones]]: H1 se sostiene como requisito previo (solo por GWA), H2 cae, H3 cae, H4 se sostiene porque cuatro métricas no maduran y dos maduran poco, H5 no se refuta ni queda demostrada, H6 cae en parte. Las Conclusiones los llevan uno por objetivo.
- **El método de análisis es posterior a los datos y anterior a cualquier coeficiente**, y se revisó después en diez puntos tras la revisión externa de la memoria, por motivos de estructura y nunca por el valor de un coeficiente. La cronología completa, con sus fechas, está en [[2 - Decisiones]] (entrada «Diez desviaciones», párrafo «Cronología») y el resumen del método en [[1 - Diseño]] §Método de análisis. La memoria lo declara en Metodología §Riesgos sin ninguna fecha, así que la cronología vive solo en este vault.
- **`results/` guarda las tablas derivadas** que leen la memoria y las figuras. La tabla larga (`tabla_larga.parquet`, los 806 que aprendieron; `tabla_larga_960.parquet`, la sensibilidad) tiene una fila por celda, ventana, predictor y variable, con la D, sus pares y su error típico por jackknife, `D_ref` contra la referencia nombrada, la granulada estratificada con sus pares, la D por hitos con el número en riesgo para la velocidad, la fracción por delante, y `D_diff` y `se_diff` (|D| de la métrica menos |D| de su referencia, con jackknife pareado) con sus `_land`; doce predictores, los once titulares más `log_lr`. Después, una tabla por lectura: `seleccion.parquet` (selección por celda y predictor, con el signo del artículo en `regret` y el invertido en `regret_flipped`), `incremental.parquet` (H2 por ventana, variable y predictor), `ventanas.parquet` y `ventanas_recuento.parquet` (H4, |D| al 50 % menos |D| al 5 % por celda con jackknife pareado; velocidad por hitos, epoch 1 frente a 2 y 2 frente a 4), `ranking.parquet` y `ranking_grupos.parquet` (H3, sobre las 24 celdas y dentro de cada conjunto y arquitectura), `optimizadores.parquet` (H5, los 12 pares que solo difieren en el optimizador) y `signos.parquet` (H6, celdas a favor y en contra del signo de cada artículo, sobre las 24 y por arquitectura). Las diez se regeneran desde `reports/` con una sola orden, en unos dos minutos.
- **Runs con NaN.** 154 nunca aprendieron (115 por colapso, 39 por divergencia) y 11 se recuperaron; los recuentos completos están en [[2 - Decisiones]] (censo de runs). El cero falso de `stiffness/sign_*` y `confusion/frac_neg` está parcheado en `reports/`, **así que se comporta como cualquier valor ausente y nada necesita una excepción**.
- **Qué hay realmente registrado por época.** `trajectory.parquet` trae **23 columnas de métrica de gradiente** (no 8: cada métrica emite varias claves) más **cuatro columnas de TSE**, más la curva cruda `train_loss`/`val_loss`/`val_acc` y los dos relojes. `metrics_at_window.parquet` son esas mismas columnas en cinco filas por run, una por ventana.
- **Memoria.** Los ocho capítulos y el anexo ODS están escritos y corregidos con la revisión externa (`writing/Revisión externa de la memoria.md`, solo en local). Metodología lleva §Protocolo de análisis y catorce riesgos. Resultados va en el orden de los objetivos (H1, H2, coste, H3, H4, H5, H6), con hoja de ruta, la tabla del accuracy de test alcanzado por celda (`tab:alcanzado`, §Cobertura) y §Conclusiones del capítulo, que cierra con la tabla por métrica `tab:aportaciones`. Conclusiones lleva una conclusión por objetivo, §Aportaciones y §Limitaciones, y responde a la pregunta en pares de cada diez. Los tres resúmenes están escritos; faltan los agradecimientos. **Ninguna fecha se imprime en el cuerpo y el anexo de configuración se borró:** la máquina vive en un párrafo de Desarrollo §Arquitectura del sistema, y nada en `reports/` la registra (`config.yaml` guarda `device: auto`), así que ese dato viene del autor. **Once figuras** en el cuerpo, nueve generadas por código y dos esquemas; la última, `composicion-celda`, en Resultados §Concordancia, es la stiffness al 5 % y las epochs hasta el umbral a lo largo del learning rate en MNIST CNN SGD, la composición de dos curvas sobre una celda real. Compila en **105 páginas de PDF**, sin cajas desbordadas y sin referencias ni citas sin resolver; el cuerpo, de Introducción a Trabajos futuros, son 88 impresas (la bibliografía empieza en la 89), por debajo de las 100 de la nota del seminario cuente lo que cuente. Las tres figuras de solape retiradas (`solape-bandas`, `solape-cruces`, `val-test`) no vuelven. Sin decidir: el sobrecoste de medición por celda como tabla, y «condición» frente a «celda» en el Estado del arte.
- **Promesas colgantes.** LaTeX no detecta ninguna, porque una referencia cruzada resuelve igual aunque la sección esté vacía, así que «cero referencias indefinidas» no vale como comprobación y hay que contarlas a mano, comparando cada etiqueta con la prosa que le sigue; escribir una sección sube tantas como baja, porque cada remisión honesta abre una nueva. Último recuento, al cerrar la fase H: cero. Queda el comentario de `main.tex` sobre revisar los resúmenes al cerrar los capítulos, que es de la fase I. Las remisiones de falsación de Introducción, Estado del arte y Fundamentos son anuncios, no errores.
- **Pilot:** ejecutado y leído; los presupuestos que salieron de él están en la configuración central y en los 24 YAML. Los umbrales viven en la configuración central, calculados sobre la matriz. `reports_pilot/` está versionado, con los seis `testfix_40ep/` de Tiny dentro.

## Vigentes: a tener en cuenta en todo lo que queda de análisis

Lista de comprobación para abrir cualquier sesión de análisis. Entra aquí lo que **condiciona un cálculo futuro** y se olvidaría con facilidad: trampas medidas, equivalencias entre columnas, y decisiones abiertas que cambian un número. No entra el estado del trabajo, que está arriba, ni el porqué de cada cosa, que está en [[2 - Decisiones]]. Una línea por asunto y un puntero. Cuando algo deja de condicionar, se borra de aquí.

- **El método es posterior a los datos.** La matriz acabó antes de que se retirara el plan de análisis. Ninguna frase, en el vault o en la memoria, puede sugerir preinscripción.
- **El umbral de VD1 es por dataset y arquitectura** ([[2 - Decisiones]]), y sale de una norma reproducible: el valor redondo más alto que alcanza al menos el 60 % de los entrenamientos que aprendieron. Si se recalcula, se recalcula con la norma y no a mano. Y **VD1 se recalcula siempre en la capa de análisis** desde `trajectory.parquet`: el campo `epochs_to_threshold` de los 960 `summary.json` está calculado con los umbrales viejos y **no se puede leer**. Lo mismo vale para `best_val_acc` y `best_val_loss`: el suavizado corregido (valor crudo en la primera y la última *epoch*) se aplica en la capa de análisis y los tres campos suavizados de `summary.json` son obsoletos.
- **El learning rate mueve el predictor y también la variable a predecir.** Un reparto de 0,90 dice que nueve décimas de esa columna son el learning rate leído de otra manera, así que cada correlación es en buena parte una correlación entre dos efectos de una causa común. Declarado en Resultados §Rango dinámico del predictor, y hay que volver a decirlo al interpretar cada contraste.
- **El predictor de referencia gana en rango dinámico a todas las métricas caras**, TSE 0,97 y la curva de validación 0,95 frente a 0,92 la mejor instrumentada.
- **VD1 y el predictor de referencia leen la misma curva.** Descontar la validación temprana para predecir VD1 es descontar un prefijo del propio resultado, y en un run ya cruzado la validación leída en la ventana vale ya el umbral. H2 se decide sobre VD4 y VD5, y hay que repetirlo al redactar OE2 y OE4.
- **El solape está medido por celda, en pares, y acota OE4 sobre la velocidad.** VD1 conserva la mitad por delante en la ventana del 5 % en 22 celdas y en la del 10 % en 12, y en ninguna al 25 % ni al 50 %. VD3 da 24, 17, 9 y 2 celdas con el suavizado corregido, y donde falla es por sobreajuste, no por velocidad. VD2 sirve hasta el 25 % en las 24 y al 50 % cae sobre la línea por construcción, «descrita a medias». **La cuenta es un suelo**, porque no haber cruzado no es estar por decidir, y la regla de la mitad describe y no decide: la lectura primaria de la velocidad es **por hitos**, la D solo entre los runs que aún no habían cruzado al cerrar la ventana, censurados incluidos, con el número en riesgo al lado; la D sobre todos los runs se publica como secundaria. Los recuentos por celda viven en la prosa de §Solape, sin figura.
- **GWA va al revés que su artículo.** Aquí se mide sobre $\nabla\ell$ bruto, así que la predicción heredada es que una GWA **baja** acompañe a mejor generalización. Conversión en Fundamentos §Métricas de alineación, con el mismo aviso en el enunciado de H6 de [[1 - Diseño]] y en la tabla de signos de [[Datos experimentales]] §5.3, que es el material contra el que se contrasta H6.
- **En los 41 runs divergidos, `final_test_acc` y `final_gap_acc` traen número y no son mediciones**, porque el índice de la clase predicha sobre logits NaN es siempre el 0. Son las dos únicas columnas donde "no falta ningún valor" engaña. Los 41 son todos de SGD: Adam no divergió nunca.
- **`noise_scale/simple` y `mcoh/global` son la misma cantidad** (GNS = M/α − 1 exacto, Spearman intra-run −1,000), y GSNR es el recíproco por parámetro de NGV. La m-coherencia sale del contraste y la escala de ruido la representa; la predicción de signo de su artículo (H6) se lee sobre la escala con el signo cambiado. NGV y GSNR no son redundantes entre sí, |D| mediana 0,59 al 5 %, pero **NGV sale también**, porque es la escala de ruido estimada con diez submuestras de 25 (Spearman 0,94 dentro de celda con la escala de ruido). La *gradient disparity* cuenta como variabilidad, porque es √(2·tr Σ/51). Quedan seis métricas de gradiente, tres por familia.
- **En velocidad, la familia primaria lee las 24 celdas por hitos** (`D_land` para VD1), con el número en riesgo declarado; la regla de la mitad ya no filtra celdas. Donde quedan pocos en riesgo, el intervalo lo dice.
- **`results/` guarda tablas derivadas y versionadas**, nunca datos crudos, y cada fichero se regenera con una orden desde `reports/`.
- **Cada métrica entra por su columna titular**, fijada en la tabla de Metodología §Variables y marcada en el código. Las demás columnas de las 27 no se contrastan, o solo como exploratorio.
- **El estadístico de todos los contrastes es la D de Somers sobre pares comparables**: valores distintos de la VD y, en velocidad, al menos un cruce; un cruce en la última *epoch* contra un censurado es comparable. Cada D lleva su error típico por jackknife (quitar un run) y un intervalo normal al 95 %; una celda muestra asociación si el intervalo excluye el cero. Agregación: cuántas celdas la muestran y con qué signo, con desglose por conjunto; el recuento de signos es descripción y la referencia binomial (18 de 24, 10 de 12) solo una vara; sin Benjamini-Hochberg.
- **H2 cayó** ([[2 - Decisiones]], fase C): ninguna métrica supera a su referencia con intervalo, ninguna es redundante con ella (|D_ref| mediana 0,23 a 0,45) y todas pierden más accuracy que la validación al elegir el learning rate (GWA 0,023 frente a 0,013). Todo lo que se escriba va con ese veredicto delante: lo que las métricas ordenan es el learning rate, y la posición en la rejilla empata con la mejor.
- **H3 cayó** ([[2 - Decisiones]], fase D): ninguna familia domina; GWA y la stiffness encabezan las tres variables y la confusión y GWA las cierran; el ganador cambia con la arquitectura (red plana alineación 7/8, convolucional variabilidad) y GWA invierte su signo con el gap entre la convolucional y la ResNet-18. La afirmación que sostiene la portada es sobre dos métricas, no sobre una familia.
- **H5 no se refutó ni quedó demostrada** ([[2 - Decisiones]], fase F): 0 a 2 inversiones de 12 por métrica, de dos en dos con las dos variables de un mismo par (disparidad en cifar10-fc, stiffness en cifar100-resnet18); lo que cambia con el optimizador es el tamaño de la D. «Invariante» no se escribe en ningún sitio; se escribe «no se ha visto lo contrario».
- **H6 cae en parte** ([[2 - Decisiones]], fase G y su revisión): de siete predicciones, dos se cumplen (GWA con test 15/1, GSNR con gap 11/2, en las tres arquitecturas), tres no reciben apoyo (confusión y escala de ruido con la velocidad, m-coherencia con el gap) y dos se invierten (stiffness con la velocidad 9 frente a 5, disparidad con el accuracy de test 12 frente a 4), las dos por composición de curvas sobre el learning rate. Concordar con el artículo no es superar a la validación, y «no superan» no es «no añaden».
- **El jackknife por run es ancho, no falso**: los ocho learning rates de una seed comparten inicialización, orden y probe, y el learning rate es fijo; quitar una seed entera da errores típicos 0,58 veces menores (cuartiles 0,38 y 0,82, reproducido dos veces) y más celdas seguras. Se mantiene el de run como intervalo único; `log_lr` no tiene varianza de seed, así que el requisito previo de H1 se lee también por signo mayoritario.
- **Siete D de la familia primaria tienen error típico exactamente cero**: Tiny-ImageNet FC Adam, velocidad por hitos al 5 %, con `log_lr`, las tres lecturas de la referencia, GSNR, disparidad y GWA. Separación perfecta entre los cinco de lr 3e-5 que cruzan en la epoch 3 y los diez censurados de 1e-3 y 3e-3; los 50 pares son todos entre los dos grupos. La celda cuenta como segura sin intervalo real; declarado en Metodología §Coeficiente por celda, Resultados §Existencia y Conclusiones §Limitaciones, con lo que baja cada recuento sin ella. No es un fallo del jackknife y no se parchea.
- **La posición en la rejilla, `log_lr`, es un predictor de coste cero en todas las tablas**: el techo de cualquier lectura monótona del *learning rate*; una métrica que no lo supera es el *learning rate* leído de otra forma.
- **H2 se compara con una referencia nombrada de antemano**, el *accuracy* de validación para VD4 y el *loss* de validación para el *gap*, y `D_ref` mide dentro de la celda si la métrica es la referencia con otro nombre; la lectura de selección da el *accuracy* de test que cuesta elegir el *learning rate* con la métrica al 5 % frente a elegirlo con la validación al 5 %.
- **Ninguna desviación posterior a la tabla larga se justifica por un coeficiente.** La cronología (método por la mañana, tabla y primera lectura de H1 por la tarde, revisión externa sin correlaciones, desviaciones después) está en [[2 - Decisiones]] y la memoria la declara en Metodología §Riesgos.
- **Las variables dependientes y los tres predictores gratuitos hacen valle o pico a lo largo del *learning rate*, y las métricas de gradiente son irregulares** (tolerancia del 5 % del recorrido). Las celdas donde una métrica monótona no puede dar relación monótona, de 1 a 7 por métrica, se declaran antes de calcular. **El censo solo aparta esos casos imposibles:** un predictor irregular no se aparta y su D puede quedar corta sin aviso; al 5 % lo son la escala de ruido en 17 celdas, GSNR en 12, GWA y la confusión en 11. El sesgo va contra las métricas, porque las tres lecturas de la referencia tienen la forma de la VD (valle o pico en 19 a 21), y la lectura de selección lo esquiva.
- **La *seed* no mueve las métricas de alineación** (0,03 a 0,08 frente a 0,13 de referencia al 5 %) **y mueve algo las de variabilidad** (0,17 y 0,12). La cuenta granulada solo tiene con qué trabajar en variabilidad; predicción registrada antes de calcularla. Es estratificada (los pares dentro de *learning rate* sumados) y lleva su número de pares.
- **Empates en la velocidad:** mediana del 13 % de los pares entre cruces por celda, máximo 44 %; los pares empatados no son comparables.
- **TSE es una suma acumulada**, no el *loss* de la *epoch*: a ventana fija ordena como la media del *loss* de las primeras *epochs*.
- **La escala de ruido pasa de su valor de referencia, 255, en una parte de la rejilla.** 255 = M − 1 es el valor al que tiende el estimador cuando el gradiente medio verdadero es cero, y no un techo: sobre los 806, el 10,9 % de las 4.022 lecturas queda por encima, con máximo 3.067, y en la ventana del 5 % lo pasan 65 con máximo 498. Ahí la mediana es 166 y el 37,8 % supera 200. Lo declaran Fundamentos, Resultados §Conclusiones del capítulo y Conclusiones §Limitaciones, con esas palabras.
- **154 runs nunca aprendieron y los 154 tienen causa conocida**, 115 de colapso y 39 de divergencia. Se identifican por el criterio «aprendió»: mejor val-accuracy suavizada por encima de **1,25 veces el azar**; 1,2 y 1,25 seleccionan los mismos 154. **Distinguir dos usos, que no son el mismo:** se excluyen del denominador que **calibra** el umbral de VD1, porque no cruzarían ninguno; la población primaria de todo **contraste** son los 806 que aprendieron y los 960 son la sensibilidad declarada.
- **La censura de VD1 se cuenta en pares, no en runs.** Un run censurado sigue siendo comparable con cualquiera que cruzó. 15 cruces de 40 conservan el 61,5 % de los pares, no el 37,5 %. Con n = 40 en las 24 celdas, esa fracción es una función estrictamente creciente del número de cruces, así que da la escala honesta y **no una evidencia aparte**, porque los dos números dicen lo mismo.
- **Los 24 YAML de `experiments/` llevan el umbral viejo por conjunto de datos.** El lanzador no pisa ficheros existentes, así que siguen ahí desde que se corrió la matriz. No afecta a ningún cálculo, porque el análisis lee los umbrales de la configuración central y nunca los YAML, pero engaña a quien los abra.
- **El suelo de ajuste del gap:** entran en VD5 y VD6 los runs cuya `final_train_eval_acc` alcanza el τ de su celda. Es un filtro de análisis, no obliga a re-correr nada, y deja 693 de los 806, con 25 por celda de mediana, 40 en cinco celdas y 20 en tres.

## Reglas de trabajo

Las que siguen mandando en lo que queda. El resto del plan por fases se cerró con la fase H y su historia está en [[2 - Decisiones]] y en git.

**Una cosa cada vez.** En cada momento hay un solo tema abierto, y no se abre otro hasta cerrarlo. Lo que aparezca por el camino se anota donde corresponda y se sigue con lo que había. Un tema avanza a la vez en el código, en el vault y en la memoria; el texto que se deja para más tarde no se escribe.

**Toda cuenta responde de qué objetivo es** y de qué se haría distinto si saliera al revés. Si no hay respuesta, no se ejecuta. Y ninguna desviación del método se justifica por el valor de un coeficiente.

**Regla de figuras:** una figura, una afirmación, con la evidencia numérica visible. Si no se puede resumir en una frase que empiece por "esta figura demuestra que", está mostrando datos y no un resultado, y su sitio es un anexo o el repositorio.

**Regla de capítulo:** la plantilla de [[Estilo de redacción - notas del TFM HOFT]] rige en los capítulos de cuerpo, del 2 al 6, y no en la Introducción, las Conclusiones ni Trabajos futuros, que cierran solos. La hoja de ruta y las conclusiones de un capítulo se escriben junto a su cuerpo y **nunca antes**, para no dejar un título sin prosa. Conclusiones no lleva ninguna cifra que no esté en Resultados.

**Regla de registro:** el vault se poda mientras se trabaja, no al final. Una entrada de [[2 - Decisiones]] guarda cuatro cosas y ninguna más: qué se decidió, por qué en la forma más corta que aún lo defienda ante un tribunal, la evidencia medida con su número, y las trampas que le costarían tiempo a quien lo toque después. Se va todo lo demás: el camino hasta la decisión, las alternativas que ya no informan nada, el detalle de la verificación, y lo que el código o la memoria ya cuentan mejor. El vault no cita funciones, commits ni sesiones de trabajo; cita datos (`reports/`, `results/`, columnas, knobs) y secciones de la memoria. **Lo que no se poda nunca son las decisiones y sus fechas**, porque son la evidencia de que el análisis es posterior a los datos y anterior a los resultados.

### Fase I: obligatorio para el depósito

Lo administrativo y lo que solo se puede hacer al final.

- [x] **Anexo ODS**, en `anexo-ods.tex`, con la tabla de los 17 y una reflexión de unas 900 palabras: 7, 9 y 12 en medio, 4 y 13 en bajo, los 12 restantes no proceden, sin ninguna cifra de energía porque solo se midieron horas de GPU. El fichero suelto para Ebrón es `thesis/anexo-ods.docx`, generado desde el `.tex` por `thesis/anexo-ods-docx.py` sobre la plantilla oficial de `docs/research/writing/`; si el `.tex` cambia, se vuelve a ejecutar.
- [ ] **Los tres resúmenes** de `main.tex` están escritos; faltan los agradecimientos.
- [ ] Formato UPV/ETSINF, Turnitin y entrega por EBRON.
- [ ] Borrador al tutor → incorporar feedback.
- [ ] Slides de defensa (10-15, ~15 min).
