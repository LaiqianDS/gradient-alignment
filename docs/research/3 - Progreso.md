## Estado actual (rev. 2026-08-28)

Aquí viven el estado y el plan. El *qué se decidió y por qué* está en [[2 - Decisiones]] y el *estado resultante del diseño* en [[1 - Diseño]]; este documento no los repite, los referencia. La retrospectiva del plan de abril a junio y las fases 0 a 6, todas cerradas o superseded, se retiraron el 2026-08-26 y siguen en el historial de git. El 2026-08-27 se deshizo la primera pasada de la fase A, código y texto incluidos, para rehacerla después de la fase 0 y con el estilo de [[Estilo de redacción - notas del TFM HOFT]] ya aplicado. **La fase 0 quedó cerrada el 2026-08-28**, con la revisión de Lai sobre el LaTeX aplicada y el capítulo de Trabajos futuros escrito. Lo siguiente es la fase A.

- **Matriz: TERMINADA.** 960 de 960 runs, las 24 celdas con sus 40 cada una, versionados en git desde el commit `f6df900` (2026-08-22), así que existen fuera de esta máquina. Coste real 121,7 h de reloj: 97,6 h de entrenamiento y 24,1 h de instrumentación, con un sobrecoste máximo por celda de 2,048x, dentro de la cota.
- **Lo único que bloquea: no hay método de análisis.** El plan preregistrado se retiró el 2026-08-25 y las seis hipótesis siguen en [[1 - Diseño]] como afirmaciones falsables **sin criterio de decisión**. Bloquea las fases B a G.
- **Sigue sin calcularse ninguna correlación entre métrica y variable dependiente.** El método de contraste se fija antes de mirar ningún resultado, y esa disciplina deja de valer en cuanto se mire una correlación antes de tenerlo escrito.
- **Código.** `src/analysis.py` reúne los diagnósticos de sanidad: validez, identidades, degeneración, tendencia y redundancia. **No hay ninguna figura ni una línea de análisis confirmatorio.** El estilo de figuras sí existe desde el 2026-08-27: `src/figstyle.py`, escrito desde cero con las notas de HOFT como referencia y con seis pruebas propias. La primera figura real llega con la fase A.
- **Qué hay realmente registrado por época.** `trajectory.parquet` trae **23 columnas de métrica de gradiente** (no 8: cada métrica emite varias claves) más **4 columnas de TSE**, más la curva cruda `train_loss`/`val_loss`/`val_acc` y los dos relojes. `metrics_at_window.parquet` son esas mismas columnas en 5 filas por run, una por ventana. Consecuencia operativa: antes de contrastar hay que fijar **una columna titular por métrica** (`analysis.py::headline_columns()` existe para eso), o la multiplicidad se dispara sin que ningún objetivo lo pida.
- **Memoria.** Terminados introducción, estado del arte y fundamentos. De metodología viven ya §Configuración del entrenamiento, §Variables, §Ventana temporal, §Matriz de experimentos, §Calibración, §Protocolo de evaluación y §Riesgos. **Solo queda vacía §Protocolo de análisis**, y acumula ya **seis** referencias entrantes de las diez colgantes; las otras cuatro apuntan a Resultados, a Conclusiones y al anexo de configuración. §Hipótesis se escribió el 2026-08-27 con las seis afirmaciones y lo que refutaría a cada una, sin criterio numérico y sin ninguna palabra que sugiera preinscripción, porque el método es posterior a los datos. Aviso, porque este documento lo dijo mal durante un tiempo: §Protocolo de análisis **no** contiene la población de análisis ni el tratamiento del censurado, que se borraron con el resto de la fase A; lo único escrito de aquello son los riesgos, en cualitativo y sin cifras. **El capítulo de Desarrollo, que hasta el 2026-08-28 se llamaba Implementación, está escrito entero** salvo su única figura, el diagrama del *pipeline*. **Trabajos futuros es capítulo propio desde el 2026-08-28 y está escrito.** Resultados y conclusiones siguen siendo títulos sin prosa. **Cero figuras.** Faltan los tres resúmenes de `main.tex`, los agradecimientos y los dos anexos.
- **Promesas colgantes en la memoria.** Las de falsación (`introduccion.tex:54` y `:68`, `estado-del-arte.tex:111`, `fundamentos.tex:170` y `:259`) las cumple la fase B y no son errores, son anuncios. Ojo: los números de línea de `fundamentos.tex` se movieron cinco posiciones el 2026-08-28, al añadir una fila a la tabla de notación y el corte de página que la mantiene en su sección. La del **frente de Pareto** quedó cerrada el 2026-08-27, y lo que se promete ahora es una comparación por clase de coste; ver §Problemas conocidos.
- **Pilot:** ejecutado y leído; presupuestos y umbrales en `config.py::DATASET_BUDGET` y en los 24 YAML. Aviso: `reports_pilot/` está en `.gitignore`, así que la evidencia que justifica esos números existe **solo en el disco local**.
- **Lista de métricas:** cerrada con la implementación. Variabilidad (normalized variance, GNS simple, GSNR) y alineación (m-coherence, stiffness, gradient disparity, gradient confusion, GWA), más TSE como baseline.

## Problemas conocidos de los objetivos (rev. 2026-08-26)

Ninguno obliga a cambiar los seis objetivos de `introduccion.tex`. Se resuelven declarando alcance, y hay que resolverlos **antes** de escribir el objetivo al que afectan.

- **OE2 y la velocidad: el baseline lee la misma curva que define la variable.** VD1 es "en qué época la accuracy de validación cruza τ", y la segunda mitad del predictor de referencia es "la accuracy de validación en la ventana f". Son la misma curva leída en dos puntos, así que descontar el baseline para predecir VD1 es descontar un prefijo del propio resultado. Hay además un solape entre la ventana de medida y el propio cruce del umbral, que **la fase A tiene que cuantificar** antes de que OE2 y OE4 se puedan escribir: si en una ventana ya ha cruzado buena parte de los runs, esa ventana no anticipa la velocidad, la describe. El contraste de OE2 será por tanto informativo sobre todo en las variables que la curva de validación no define, es decir, la accuracy de test y los dos gaps.
- **OE3 y el solape entre familias.** `noise_scale/simple` (variabilidad) y `mcoh/global` (alineación) son la misma cantidad reparametrizada: `GNS = M/α − 1` exacto (Ecuación `eq:gns-mcoh` de `fundamentos.tex`), y el Spearman intra-run medido sobre los 960 sale **−1,000**. Además GSNR es el recíproco por parámetro de NGV. Comparar familia contra familia sin podar antes cuenta el mismo número una vez en cada bando. **La poda con prueba deja de ser mejora de presentación y pasa a ser requisito previo de OE3.**
- **El coste por métrica no está medido y no es separable.** `summary.json` trae un único `metric_seconds` por run, y `metrics_runner.measure` construye **un solo** barrido per-sample que consumen seis de las ocho métricas, así que su coste marginal individual no existe ni a posteriori. El objetivo general prometía "la mejor capacidad predictiva por unidad de coste" y había un frente de Pareto prometido en `estado-del-arte.tex:111` con sección propia en `resultados.tex`. Con los 960 runs **no es computable**. Dos salidas honestas: apoyarse en la columna de requisito de cálculo que ya publica `tab:sota-metricas`, o medir el coste por métrica con un micro-benchmark aparte, minutos y fuera de la matriz. **Resuelto el 2026-08-27 por la vía del coste asintótico** (entrada en [[2 - Decisiones]]): `estado-del-arte.tex:111` ya no promete un frente, y §Frente de Pareto pasa a §Coste y capacidad predictiva (`sec:res-coste`). La sección se mantiene en lugar de doblarse dentro de OE2, porque el coste asintótico le da material propio, las clases y quién lidera cada una, mientras que OE2 pregunta otra cosa, si alguna métrica supera al predictor de referencia. **Decisión mía, revisable.** El objetivo general de `introduccion.tex:43` pasa de "por unidad de coste", que con clases discretas no es literal, a "a un coste justificado", que es la fórmula que ya usaba OE2.

## Plan por objetivos (rev. 2026-08-26; objetivo: primera semana de septiembre de 2026)

**Metodología de trabajo, fijada el 2026-08-27: una cosa cada vez, y en tres carriles a la vez.** Son dos mitades que se sostienen la una a la otra, y gobiernan todo lo que queda del proyecto.

*Una cosa cada vez.* En cada momento hay un solo tema abierto, y no se abre otro hasta cerrarlo. Lo que aparezca por el camino se anota donde corresponda y se sigue con lo que había. Dispersarse deja tres cosas al setenta por ciento, y tres cosas al setenta por ciento valen cero.

*Tres carriles a la vez.* Ese tema único avanza en los tres sitios en la misma tanda: el **código** de `src/` y `tests/`, la **documentación** del vault, y la **memoria** en `thesis/`. No se adelanta el código para escribir el texto más tarde, porque el texto que se deja para más tarde no se escribe. Si un paso no toca alguno de los tres carriles, se dice por qué en vez de dejarlo en silencio.

**La regla de fase, y no se negocia: una fase, un objetivo, un entregable completo.** Una fase se cierra cuando su código está escrito y probado, sus números calculados sobre `reports/`, su figura hecha, y su texto redactado en el `.tex` correspondiente. Nada pasa a la fase siguiente con algo a medias, porque lo que queda a medias es lo que no se hace.

**Segunda regla, del 2026-08-26:** toda cuenta que se ejecute tiene que poder responder de qué objetivo es y qué se haría distinto si saliera al revés. Si no hay respuesta, no se ejecuta. Y el contraste no puede ser solo visual: cada objetivo lleva su prueba de hipótesis, que hay que reconstruir porque la anterior se retiró.

**Regla de figuras, del 2026-08-26:** una figura, una afirmación, con la evidencia numérica visible. Si no se puede resumir en una frase que empiece por "esta figura demuestra que", está mostrando datos y no un resultado, y su sitio es un anexo o el repositorio.

**Esto es estructura, no compromiso.** Lo único fijo son las reglas de arriba. Todo lo que sigue es un esqueleto para no perderse, y **cada fase puede cambiar**: su método, su contenido, sus entregables, su orden, y una fase puede partirse en dos o desaparecer. Cada fase se abre decidiendo qué se hace en ella y registrando esa decisión en [[2 - Decisiones]] antes de programar nada. Si al abrir una fase resulta que las siguientes ya no tienen sentido tal como están escritas aquí, se reescriben aquí y se sigue: este documento va detrás del trabajo, no delante.

### Fase 0: poner el LaTeX al día (paralela a la fase A)

No depende de ningún resultado, así que corre en paralelo. Recoge en la memoria todo lo que hoy ya se sabe, para que el análisis se escriba sobre un documento al día y no sobre uno a medias.

- [x] **Plantilla de capítulo de HOFT.** Hecha el 2026-08-27. **Norma:** la plantilla rige en los capítulos de cuerpo, del 2 al 6, y no en la Introducción ni en las Conclusiones, que ya cierran con «Estructura de la memoria» y con la memoria misma; HOFT tampoco las incluye. Ese papel lo hace «Posicionamiento de este trabajo» en Estado del arte, y Fundamentos tiene sección propia; faltan las de Metodología, Implementación y Resultados, que se escriben junto al cuerpo de su capítulo y nunca antes, para no dejar un título sin prosa. La instrucción queda como comentario en esos tres ficheros. Los tres capítulos escritos ya abrían con su hoja de ruta, así que el trabajo real fue poner al día la de la Introducción, que nombraba §Amenazas a la validez tras el renombrado a §Riesgos, llamaba «rejilla» a la matriz de experimentos y se saltaba dos secciones de nueve.
- [x] **Estilo de figuras definido desde cero el 2026-08-27**, en `src/figstyle.py`, con las notas de HOFT §Estilo de las figuras como referencia y aprobado por Lai sobre una demo con datos inventados. Decisión completa en [[2 - Decisiones]]. Lo esencial: la figura se genera al ancho final y LaTeX no la escala, salida siempre en PDF, ningún eje cortado por encima del cero, paneles de la misma cantidad con la misma escala, y el color nunca lleva información él solo. Seis pruebas nuevas fijan esas promesas, suite en **227** verdes. **Ninguna ayuda para gráficas concretas, a propósito:** la primera figura real llega con la fase A.
- [x] **Unificar la terminología en inglés.** Hecho el 2026-08-27, decisión y detalle en [[2 - Decisiones]]. A inglés *epoch*, *learning rate* y *seed*; "conjunto de medición" pasa a "batch de medición" y queda definido en `fundamentos.tex` antes de usarse; todo anglicismo va en cursiva **siempre**, que es la norma de la escuela y deroga la convención anterior de cursiva solo en la primera aparición.
- [x] **Cerrado el 2026-08-27: el capítulo de Metodología está escrito entero salvo §Protocolo de análisis, que es de la fase B.** Secciones que no dependen del análisis: variables, ventana temporal, matriz de experimentos y protocolo de evaluación. **§Configuración del entrenamiento terminada el 2026-08-27**: estaba a medias y la lista no lo recogía, porque describía los conjuntos de datos y daba por descritos los otros dos factores que §Matriz dice que están ahí. Añadidos el preprocesado (normalización con estadísticos del entrenamiento, y por qué no hay *data augmentation*), las tres arquitecturas y los dos optimizadores, con los valores por defecto de Adam escritos por primera vez. Corregido de paso el orden: la tabla de *accuracies* de referencia por arquitectura llegaba antes de que ninguna arquitectura estuviera presentada. **§Matriz de experimentos escrita el 2026-08-27**: la celda como unidad de análisis, por qué el presupuesto va a los *learning rates* y no a las *seeds*, rejilla por optimizador y no por arquitectura, extremos divergentes como recorrido deliberado del eje de eficiencia, y perillas congeladas; con la tabla «Ejes de la matriz de experimentos». Cifras verificadas contra `src/config.py`, que es la fuente única de la rejilla. **§Variables escrita el 2026-08-27**: las seis VD agrupadas en tres constructos con una principal y una comprobación cada uno, la mediana móvil de tres *epochs* que `fundamentos.tex` prometía a este capítulo y no estaba escrita, el censurado registrado como ausencia, las ocho métricas más el predictor de referencia con la lista cerrada antes de ejecutar, y las 27 columnas de predictor como hecho de las variables. **§Ventana temporal escrita el 2026-08-27**: la ventana como fracción del presupuesto y no como número fijo de *epochs*, el acoplamiento con el redondeo de §Calibración (con 20 y 40 las cuatro fracciones caen en *epoch* entera; la más temprana es una *epoch* en MNIST y dos en el resto), el barrido como resultado y no como detalle, y la condición de que una ventana solo predice lo que ocurre tras cerrarse, que para la velocidad no está garantizada. **§Protocolo de evaluación escrita el 2026-08-27**, y con ella se cierra este punto: papeles sin cruces de las tres particiones, por qué tres y no dos (con dos, el umbral que define la velocidad se habría calibrado sobre la curva que luego lo mide), presupuesto fijo sin *early stopping*, el término de entrenamiento del *gap* sobre un subconjunto del tamaño del test, y dos de las tres notas de honestidad que la fase H tenía apuntadas: F1 macro como comprobación y partición fija a propósito. La tercera, el test de Tiny-ImageNet, ya estaba dicha en §Configuración y Lai pidió no repetirla. **§Hipótesis escrita el mismo día**, con las seis afirmaciones y lo que refutaría a cada una. Al redactar H6 apareció una contradicción interna que llevaba meses en el texto: `fundamentos.tex:168` convierte con cuidado el signo de GWA, porque el artículo original define el gradiente como $-\nabla\ell$ y aquí se mide sobre $\nabla\ell$ bruto, de modo que la predicción heredada pasa a ser que una GWA **baja** acompañe a mejor generalización; dos líneas después, la frase de resumen del capítulo listaba "más alineación gradiente-peso" entre lo que acompaña a un entrenamiento eficiente, y deshacía la conversión. Corregida la frase de resumen, y H6 escrita contra el signo ya convertido. **Cuidado al tocar cualquier texto sobre GWA: la dirección correcta en esta memoria es la contraria a la del artículo.**
- [x] **Capítulo de implementación escrito el 2026-08-27** (renombrado a **Desarrollo** el 2026-08-28), de seis títulos sin prosa a capítulo completo con su hoja de ruta y sus conclusiones. Cinco secciones: arquitectura del *pipeline* (una configuración, un programa, cuatro ficheros de salida y el resumen escrito el último), cálculo de las métricas (interfaz única, núcleo puro separado de la extracción de gradientes, barrido compartido, los 14 GB que nunca se materializan, y el aislamiento de fallos), reproducibilidad (dos semillas distintas, *batch* de medición congelado, y el límite declarado del decodificador JPEG en Tiny-ImageNet), ejecución y reanudación (idempotencia por fichero marcador, troceado para una sola GPU y no para un clúster), y piloto (solo el aislamiento de salidas). **Falta su única figura**, el diagrama del *pipeline*, que espera al estilo de figuras; queda como comentario en el fichero. Suite verificada al escribirlo: **227 pruebas verdes**, no las 241 que dice `CLAUDE.md`, cifra que quedó obsoleta con el deshacer de la fase A.
- [x] Revisar §Protocolo de análisis y §Riesgos: qué se queda, cómo se llama y qué se va al capítulo de resultados por estar duplicado. **Revisión hecha, y el resultado es que en §Protocolo de análisis no queda nada que revisar**: está vacía desde el deshacer de la fase A, así que no hay duplicación que repartir ni nombre que discutir, solo texto que escribir en la fase B. **§Riesgos escrita el 2026-08-27** con cinco riesgos y ninguna cifra: la agregación de celdas, el solape entre ventana y desenlace, la inexistencia de la variable de velocidad en parte de la matriz, la no independencia de las dos familias, y los límites de alcance con el carácter correlacional del diseño. Se dejaron fuera a propósito dos riesgos reales que dependen de decisiones abiertas: que el predictor de referencia lee la misma curva que define la velocidad, y que el coste por métrica no es separable. Queda §Protocolo de análisis, que la fase B escribe.
- [x] Cerrar las promesas colgantes que ya se pueden cerrar sin tener el método de análisis. **Revisadas las tres el 2026-08-27, y cerradas las tres.** La de `fundamentos.tex` ("las predicciones con signo se formalizan como hipótesis falsables en el Capítulo 4") se cumplió sola al escribir §Hipótesis. La de `introduccion.tex` prometía que cada hipótesis lleva "un criterio de falsación explícito", y §Hipótesis da lo que refutaría a cada una pero no el número: reescrita para decir eso mismo y remitir el criterio numérico al protocolo de análisis, sin debilitar la promesa. La del **frente de Pareto** (`estado-del-arte.tex:111`) dependía del coste por métrica, y se cerró en dos pasos. Primero el coste asintótico, derivado y escrito en la tabla `tab:coste-metricas` de `implementacion.tex` §Cálculo de las métricas, con las derivadas y la aritmética de cada métrica en los dos regímenes. Después la frase, que ya no promete un frente sino una comparación dentro de cada clase de coste y contra el predictor que no deriva nada; con ella cambiaron la sección de resultados, ahora §Coste y capacidad predictiva, y el comentario de `conclusiones.tex`. Decisión completa en [[2 - Decisiones]], entrada del 2026-08-27.
- [x] **Criterio de cierre:** ninguna sección con título y sin prosa salvo las que dependen de resultados, y ninguna referencia cruzada que apunte a una sección vacía. **Cumplido en la parte que toca a la fase 0**, y el resto queda atribuido a fases posteriores. **Medido el 2026-08-27, al cerrar Metodología e Implementación: 10 referencias apuntan a algo vacío**, frente a las 13 del arranque de la jornada. El reparto es ya limpio y dice exactamente qué falta: **cinco a §Protocolo de análisis**, que escribe la fase B; **tres a los capítulos de Resultados y Conclusiones**, que dependen de resultados y por tanto están exentas del criterio; una a `sec:determinismo` del anexo de configuración, que es escritura administrativa de la fase I; y una más de la Introducción a Conclusiones. Es decir, **ninguna de las diez es ya trabajo de fase 0**. Lección del recuento: escribir una sección lo sube tanto como lo baja, porque cada remisión honesta abre una colgante nueva; §Matriz cerró dos y abrió una, §Variables cerró una y abrió dos, y el capítulo de Implementación cerró una y abrió otra hacia el anexo. El recuento se reproduce con el script del scratchpad de la sesión, que compara cada `\label` con la prosa que le sigue. Ojo: LaTeX no detecta ninguna de las dos cosas, porque un `\ref` resuelve igual aunque la sección esté vacía, así que «cero referencias indefinidas» no vale como comprobación y hay que contarlas aparte. **Recontadas el 2026-08-28: siguen siendo diez, pero cambia el reparto.** §Protocolo de análisis sube de cinco a seis, porque la nueva Tabla 4.1 remite allí para la forma definitiva de la tabla de análisis, y la familia de Conclusiones baja de cuatro a tres, porque la remisión de `introduccion.tex:59` sobre la intervención ya apunta a Trabajos futuros, que está escrito. El reparto exacto es seis a §Protocolo de análisis, una a `ch:resultados`, dos a `ch:conclusiones` y una a `sec:determinismo`.

- [x] **Revisión de Lai sobre el LaTeX, aplicada el 2026-08-28.** De estructura: el capítulo de Implementación pasa a llamarse **Desarrollo** (fichero, etiqueta y las dos etiquetas de sección incluidas) y **Trabajos futuros sale de Conclusiones a capítulo propio**, las dos cosas para seguir el esqueleto del apartado 1 de [[Seminarios TFG - cosas a tener en cuenta]], que es el genérico y no el ejemplo del GII. El índice queda ya con los ocho capítulos del seminario, más bibliografía y anexos. De palabras: fuera «instrumentar» el gradiente en nueve sitios, a «calcular» o «medir»; «perillas» a «ajustes»; «operacionalizar» sustituido por el verbo que toca; y **33 punto y coma de prosa reconstruidos en frases**, de modo que los que aparecen hoy en un grep están todos dentro de comentarios. De claridad: la descomposición de Chatterjee dice ya qué es cada mitad, GWA dice qué capa es la última capa, la varianza normalizada dice por qué se divide, H4 da las *epochs* concretas de cada ventana, H5 explica qué es el sentido de una relación antes de usarlo, y **H6 pasa de «concordancia con el mecanismo» a «concordancia con la literatura»**, que es como ya se llamaban OE6 y su sección de resultados. La columna «Derivadas» de `tab:coste-metricas` pasa a «Pasadas hacia atrás», y el párrafo que presenta la tabla explica qué es una, porque el pie es una etiqueta y no una explicación. **Nueva Tabla 4.1, «Bloques de la tabla de análisis»**, al final de §Variables: la unidad es un entrenamiento leído en una ventana, o sea 4.800 filas, con las columnas repartidas en cuatro bloques y su tipo, y con la advertencia de que la forma definitiva es decisión de la fase B. Dos errores encontrados de paso y corregidos: §Variables decía que GWA emite «una o dos» columnas cuando emite tres, y decía «las seis quedan definidas en §Eficiencia» mientras enumeraba siete, porque la F1 macro se define en §Protocolo de evaluación. **Precio pagado:** la tabla de notación no cabe en lo que la apertura del capítulo 3 deja libre, ni siquiera a cuerpo menor, así que se fuerza con `\clearpage` para que no caiga dentro de §3.2; cuesta dos páginas y deja la de apertura a un tercio. Compila en 59 páginas, sin referencias sin resolver ni cajas desbordadas.
- [x] **Capítulo de Trabajos futuros escrito el 2026-08-28**, y con él se cierra la fase 0. Al rastrear qué difiere la memoria al futuro aparecieron dos promesas que no estaban en los apuntes de Conclusiones: la variante de signo de la *stiffness*, que el estado del arte deja «abierta a un futuro estudio en condiciones menos restrictivas», y la partición fija, que §Protocolo de evaluación llama «pregunta legítima pero distinta». Tres secciones: intervención sobre la señal, con las dos condiciones que exige (un criterio de decisión, que depende de lo que responda H2, y un diseño experimental propio, porque intervenir altera la trayectoria que se observa y los 960 no se pueden reanalizar); extensión del alcance (otros dominios y *transformers*, ImageNet completa, *data augmentation*, y partición aleatorizada); y medición y métricas (variante de signo, GNS exacta con la Hessiana, régimen post-meseta, coste por métrica no separable, y el tiempo de reloj hasta el umbral que ya está registrado sin ser variable del estudio). **El régimen post-meseta va en cualitativo y sin las cifras del piloto, por decisión de Lai:** los porcentajes viven en `reports_pilot/`, que está en `.gitignore`, así que meterlos ataría la memoria a evidencia que no se puede reproducir desde el repositorio. Si se quieren dentro, la copia de seguridad de §Higiene deja de ser higiene y pasa a ser requisito previo.

### Fase A: datos y validez

No ataca ningún objetivo. Establece que los datos sirven y qué se puede calcular con ellos. **Regla de ejecución: un lado cada vez**, predictores y variables dependientes por separado.

- [ ] Validez de las columnas registradas sobre los 960 runs: rango teórico, identidades exactas y columnas ausentes, valores nulos o imposibles, runs que se quedaron sin aprender estancadas, etc.
- [ ] Mapa de lo computable celda a celda: qué runs entran en el análisis y cuántos quedan para cada variable dependiente.
- [ ] Rango dinámico del predictor: si la métrica se mueve más por el learning rate que por la semilla.
- [ ] Solape entre la ventana de medida y el cruce del umbral, que decide en qué ventanas se puede analizar la velocidad.
- [ ] Concordancia entre validación y test, que decide si las variables leídas sobre la curva de validación sirven.
- [ ] **Criterio de cierre:** veredicto de validez escrito, mapa tabulado, y las decisiones que se deriven reflejadas en el `.tex`.

### Fase B: OE1, existencia

- [ ] Decidir el método y registrarlo. Programarlo con pruebas sobre datos sintéticos de efecto conocido, antes de tocar `reports/`.
- [ ] Aquí se reconstruye la **prueba de hipótesis**, una sola vez y para todos los objetivos: qué se calcula dentro de cada celda, cómo se agregan las 24, cómo entra el censurado y cómo se corrige la multiplicidad.
- [ ] **Texto:** §Hipótesis (H1) de `metodologia.tex`, hoy vacía, y la parte de contraste de §Protocolo de análisis, más su sección de resultados.
- [ ] **Criterio de cierre:** H1 respondida con su número, su figura y su párrafo.

### Fase C: OE2, valor incremental (el decisivo)

- [ ] Método para descontar el predictor de referencia (TSE y validación temprana), con la asimetría de §Problemas conocidos declarada por escrito.
- [ ] El eje de coste tiene sección propia, §Coste y capacidad predictiva; aquí entra solo en cuanto OE2 pregunta si ese coste está justificado.
- [ ] **Criterio de cierre:** H2 respondida sobre las seis VD, con la limitación de la velocidad declarada, no omitida.

### Fase D: OE3, comparación de familias

- [ ] **Requisito previo:** poda con prueba del par `noise_scale/simple` ≡ `mcoh/global`, y del solape NGV/GSNR. Sin poda, este objetivo no es contestable.
- [ ] **Criterio de cierre:** H3 respondida sobre la lista podada, y la poda documentada como resultado propio.

### Fase E: OE4, suficiencia temprana

- [ ] Barrido de ventanas 5/10/25/50 %, teniendo en cuenta el solape medido en A4.
- [ ] **Criterio de cierre:** H4 respondida, con la ventana mínima defendible dicha explícitamente.

### Fase F: OE5, robustez entre optimizadores

- [ ] Comparación pareada sobre los 12 pares de celdas que solo difieren en el optimizador.
- [ ] **Criterio de cierre:** H5 respondida, declarando que 12 pares dan poca potencia y que un no rechazo no prueba invariancia.

### Fase G: OE6, concordancia con la literatura

- [ ] Contraste del signo observado contra el predicho por cada paper. La tabla de signos ya está verificada contra los PDFs (2026-07-17, [[Datos experimentales]] §5.3) y la salvedad de signo de GWA está en `fundamentos.tex:168`.
- [ ] **Criterio de cierre:** H6 respondida, separando los signos que el paper afirma de los que esta memoria extrapola.

### Fase H: cierre de la memoria

- [ ] **Conclusiones**, una por objetivo.
- [ ] **Cerrar las promesas colgantes** listadas en §Estado actual, incluida la decisión sobre el frente de Pareto.
- [ ] **Declarar el encuadre del análisis:** posterior a los datos y anterior a los resultados, con las fechas que lo sostienen.
- [ ] **Notas de honestidad:** Tiny-ImageNet usa su val público como test, F1-macro ≈ accuracy en datasets balanceados, y el split es fijo y compartido por todos los runs.

### Fase I: obligatorio para el depósito (independiente, en cualquier hueco)

No depende de ningún resultado, así que puede adelantarse desde ya. Lo que es escritura de capítulos se ha movido a la fase 0; aquí queda solo lo administrativo y lo que solo se puede hacer al final.

- [ ] **Los tres resúmenes** de `main.tex`, los agradecimientos, y los **dos anexos** (ODS, obligatorio, 1-2 páginas; configuración y reproducibilidad).
- [ ] Formato UPV/ETSINF, Turnitin y entrega por EBRON.
- [ ] Borrador al tutor → incorporar feedback.
- [ ] Slides de defensa (10-15, ~15 min).

### Higiene, barato y en cualquier hueco

- [ ] **Copia de seguridad de `reports_pilot/`.** Está en `.gitignore` y solo existe en el disco local: si se pierde, se pierde la evidencia que justifica los presupuestos y umbrales congelados.
