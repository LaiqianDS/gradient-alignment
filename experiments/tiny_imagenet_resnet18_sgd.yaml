# Matrix cell: tiny_imagenet x resnet18 x sgd.
# Generated by `python src/run_matrix.py --init`.
# Edit the budget here if a pilot says so; lr and seed are the
# sweep axes and are injected by the launcher -- do NOT pin them.
dataset: tiny_imagenet
model: resnet18
optimizer: sgd
epochs: 80
threshold_acc: 0.25
batch_size: 128
momentum: 0.9
weight_decay: 0.0
probe_size: 256
windows:
- 0.05
- 0.1
- 0.25
- 0.5
- 1.0
