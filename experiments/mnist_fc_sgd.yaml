# Matrix cell: mnist x fc x sgd.
# Generated by `python src/run_matrix.py --init`.
# Edit the budget here if a pilot says so; lr and seed are the
# sweep axes and are injected by the launcher -- do NOT pin them.
dataset: mnist
model: fc
optimizer: sgd
epochs: 20
threshold_acc: 0.97
batch_size: 128
momentum: 0.9
weight_decay: 0.0
probe_size: 256
windows:
- 0.05
- 0.1
- 0.25
- 0.5
- 1.0
